# PROJECT00_README.md — MOVED (pointer, not a copy)

**This file is a pointer. The register lives at
`workstreams/curriculum-foundations/PROJECT00_README.md`.**

**§3 of that file is the home of the initiative-wide master `D-###` series** — the register this
copy was imported for (CD-035).

## Why

`workstreams/curriculum-foundations/` is now the canonical home of the Project-00 registers. The
READ-ONLY copy that lived here is retired to a pointer rather than kept as a second copy: canon and
registers are **cited, never copied** (AGENTS.md §8).

## What the retired copy contained — recorded before replacement

Byte-for-byte the staged Project-00 README **plus the CD-034/CD-035 REFERENCE IMPORT banner**, and
nothing else. Verified at source before replacement:

- **Last updated** `2026-07-17` — identical on both sides.
- **Master `D-###` range** — defined through **`D-053`** on both sides. No master row existed here
  that does not exist in `curriculum-foundations/`.
- The only differing lines were the banner.

## The two registers, kept apart (CD-034)

- **`D-###`** — initiative-wide master → `curriculum-foundations/PROJECT00_README.md` **§3**.
- **`D-PROJ00-###`** — Project 00's local register →
  `curriculum-foundations/PROJECT00_DECISIONS.md`.

A `D-###` citation resolves in §3 of the README, never in the DECISIONS file, and the reverse.
