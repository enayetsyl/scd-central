# PROJECT00_DECISIONS.md — MOVED (pointer, not a copy)

**This file is a pointer. The register lives at
`workstreams/curriculum-foundations/PROJECT00_DECISIONS.md`.**

## Why

The P00 fold-in workstream now exists. `workstreams/curriculum-foundations/` is the **canonical
home** of the Project-00 registers, so the READ-ONLY copy that lived here under CD-034/CD-035 is
retired to this pointer rather than kept as a second copy. Canon and registers are **cited, never
copied** (AGENTS.md §8); two copies of one register is the defect that rule exists to prevent.

## What the retired copy contained — recorded before replacement

Byte-for-byte the staged Project-00 register **plus a 17-line CD-034 REFERENCE IMPORT banner**, and
nothing else. Verified at source before replacement:

- **Version** `v1.60`, **Last updated** `2026-07-17` — identical on both sides.
- **`D-PROJ00-###` range** — max `072` on both sides. No row existed here that does not exist in
  `curriculum-foundations/`.
- `diff` reported **17 lines present only in this copy and 0 lines present only in the staged
  file**; all 17 were the banner.

The banner's operative sentences are preserved here because they still govern:

> **REFERENCE IMPORT — read-only, never continued from here (CD-034).** Imported to `scd-central`
> 2026-08-09 as a **read-only reference**. It is **cited, never extended**: new `D-PROJ00-###` rows
> belong to the future **P00 fold-in workstream**, not here.
>
> ⚠️ **This is Project 00's LOCAL register (`D-PROJ00-###`), not the master D-series.**

**The fold-in workstream the banner names is now `workstreams/curriculum-foundations/`.** Whether
that workstream may *extend* the register — as opposed to holding it read-only — is a Principal
ruling and is **not settled by this pointer**.

## Reading the two series apart (CD-034's two-register rule, unchanged)

- **`D-PROJ00-###`** — Project 00's local register → `curriculum-foundations/PROJECT00_DECISIONS.md`.
- **`D-###`** — the initiative-wide master series → `curriculum-foundations/PROJECT00_README.md` §3.

Do not conflate them.
