SELFTEST — both families, before any verdict (CD-025). Synthetic fixtures only; no canon/sources or canon/marklogic file is read as fixture data.

--- QUESTION_BANK_POLICY §5 family --------------------------------------------
SELFTEST — the instrument is proven before any bank verdict (CD-025)
  PASS  FLAG-TRACE stays quiet on a FLAGGED row (CD-042: FLAGGED is promotable)
  PASS  baseline: an unbroken bank is CLEAN
  PASS  POOL-MEMBERSHIP  fires on: question in two pools
  PASS  POOL-MEMBERSHIP  fires on: a CW pool reappears
  PASS  ZERO-OVERLAP     fires on: same stem in HW and CT
  PASS  ZERO-OVERLAP     fires on: exact duplicate INSIDE the HW pool
  PASS  ANSWER-SHAPE     fires on: an MCQ with two correct options
  PASS  ANSWER-SHAPE     fires on: an MCQ with no correct option
  PASS  ANSWER-SHAPE     fires on: a distractor with no why_wrong
  PASS  SOURCE-TRACE     fires on: a one-word anchor that matches anything
  PASS  SOURCE-TRACE     fires on: a real span that has nothing to do with the item
  PASS  HONORIFIC        fires on: bare নবি with no (স)
  PASS  HONORIFIC        fires on: হজরত with no (স)
  PASS  RUBRIC-SPECIFICITY fires on: two S08 items sharing one rubric
  PASS  TOPIC-NUMBER     fires on: a topic_tag that is in no chart row
  PASS  FLAG-TRACE       fires on: a flag pointing at a tag that does not exist
  PASS  FLAG-TRACE       fires on: a flag naming a row that is OPEN (Principal-owed)
  PASS  FLAG-TRACE       fires on: a flag with no closes_on
  PASS  MARK-VALUE       fires on: MCQ carries 5 marks
  PASS  SOURCE-TRACE     fires on: an item with no extraction anchor
  PASS  QUOTE-VERBATIM   fires on: an invented quotation
  PASS  HONORIFIC        fires on: the Prophet's name without (স)
  PASS  AS-MIX           fires on: AS is entirely recall
  PASS  SCRIPT-GUARD     fires on: Arabic script in a stem
  PASS  SCRIPT-GUARD     fires on: an arrow in a stem
  PASS  NUMERALS         fires on: an ASCII digit in a stem
  PASS  CEILING          fires on: HW above its ceiling
  PASS  SOURCE-EXCLUSION FAILs when an item's qid resolves to a declared-excluded chapter (`U12`) — CD-127(b) given an executor
  PASS  SOURCE-EXCLUSION FAILs on the ZERO-PADDED spelling `U012` too — the exclusion fails CLOSED, which is the opposite direction from CD-088 and the safe one
  PASS  SOURCE-EXCLUSION control · a NON-excluded chapter (`U21`) stays silent — a gate that fires on everything is as useless as one that fires on nothing
  PASS  SOURCE-EXCLUSION control · the same chapter number in a DIFFERENT subject/class is not excluded — the declaration is (subject, class, chapter), not a bare number
  PASS  SOURCE-EXCLUSION FAILs when the bank's whole declared SOURCE FILE is excluded, even with no matching qid
  PASS  SOURCE-EXCLUSION a MALFORMED declaration FAILs rather than being skipped — a prohibition nobody can resolve is not a prohibition (§7.17: reports or refuses, never omits)
  PASS  SOURCE-EXCLUSION with NO declaration anywhere the gate is silent but SAYS SO — it never passes by reporting nothing
  PASS  SOURCE-EXCLUSION a STANDALONE declaration comment is read — the mint
  PASS  SOURCE-EXCLUSION the SAME string quoted inside a table row is NOT read — a CD row that shows the form must not mint an exclusion (AGENTS §5.1, ledger_check's mint-vs-cite anchor)
  PASS  SOURCE-EXCLUSION the same string inside a FENCED BLOCK is NOT read — P-035's policy clause has to be able to print its own convention
  PASS  CHAPTER-RESOLVE  `U09` and `U9` are TWO chapters when the extraction prints both — not merged
  PASS  CHAPTER-RESOLVE  an EXACT match reports no note — the fallback stays out of the way
  PASS  CHAPTER-RESOLVE  `U09` against an extraction printing only `পাঠ ৯` resolves on the unpadded spelling AND says so (P-034 evidence is printed, not absorbed)
  PASS  CHAPTER-RESOLVE  the fallback runs ONLY for a padded token: `U9` against a padded-only extraction does not silently grab `পাঠ ০৯`
  PASS  CHAPTER-RESOLVE  a unit with no section at all falls back to the whole file, as before
SELFTEST RESULT: PASS (25 seeded errors + 10 CD-131 exclusion cases + 5 CD-130(a) resolver cases + 1 baseline)

--- canon/QUESTION_POLICY §6 family -------------------------------------------
SELFTEST — the instrument is proven before any bank verdict (CD-025). Synthetic fixtures only; no canon/sources or canon/marklogic file is read as fixture data (CD-055, CD-064(f)).
  PASS  baseline: an unbroken synthetic chapter bank passes all eleven INCLUDING PLAN
        (PLAN was EXCLUDED from this baseline until 2026-08-18, and the exclusion was arithmetic rather than a waiver: a 24-item pool could not clear four positive floors with margin 2, so the minimum was 40. CD-171(d) retires the margin rule, PLAN computes no counts, and the exclusion is DELETED — the 24-item bank now passes every gate in the family with nothing held out. An exclusion kept past its reason is how a waiver gets built by accident.)

  PASS  MARK-VALUE     fires on: an S05 mcq carrying 5 marks instead of the spine's 1
  PASS  SOURCE-TRACE   fires on: an anchor that appears nowhere in the extraction
  PASS  SCRIPT-GUARD   fires on: tier-1 Arabic script in a stem
  PASS  REF19-SLUG     fires on: a ref19_topic_id that is in no REF-19 row
  PASS  TOPIC-NUMBER   fires on: an unminted topic_tag — must FAIL and must not auto-mint
  PASS  KEY-RUBRIC     fires on: a descriptive item whose rubric has one band
  PASS  BLOOM-BAND     fires on: an item tagged `Synthesis` — not one of the six LOCKED levels. CD-171 retires the DISTRIBUTION and keeps the TAG: recorded, not rationed, and an unrecordable level is not a recording
  PASS  BLOOM-BAND     fires on: an item with NO `bloom_level` at all — the same rule from the other side, seeded separately because a missing key and a wrong value reach the gate by different paths
  PASS  DIFFICULTY     fires on: the pool cannot supply easy ≥30%
  PASS  REPETITION     fires on: an identical stem on two Understand items
  PASS  COVERAGE       fires on: an item sitting in S14 — paper-level for EVERY chapter (CD-147), so this is categorical and NOT the CD-138(e) 'this chapter declared it inadmissible' failure. The two are reported as different things because they have different remedies: one re-declares, the other moves the item to the paper pipeline
  PASS  COVERAGE       fires on: a header that ADMITS S14 — CD-147 puts it outside what a chapter declaration is entitled to say at all, so admitting it is a failure even with no item in it
  PASS  ENVELOPE-SYNC  fires on: the bank gains an item and the export does not — the export is BEHIND, which is exactly the wave-2 shape: 36 envelopes against 88 then 110 bank items, for two waves, seen by nothing
  PASS  ENVELOPE-SYNC  fires on: an item is RETIRED from the bank and its envelope survives — a stale ADDITION is loud, a stale SURVIVAL is silent, and the silent one is the one that reaches the Hub. Seven such orphans were found at the real regeneration, five of them retired S10 ভাব নির্ণয় items
  PASS  COVERAGE       fires on: ten items in S10 doing ভাব নির্ণয় — a task admitted at NO class in the whole spine. THIS IS THE LIVE DEFECT THE REGISTER WAS BUILT FOR: it passed the old slot-id-presence reading, because the id was there
  PASS  COVERAGE       fires on: an OFF-CHOICE item — ক্রিয়ার কাল is in S10's admitted_set, but C5 selected পদ নির্ণয়. A different failure from the one above and reported as one
  PASS  COVERAGE       fires on: an item sitting in a slot THIS CHAPTER DECLARED INADMISSIBLE (CD-138(e))
  PASS  COVERAGE       fires on: a COMPOSITE slot done by halves — the item breaks the যুক্তবর্ণ and never forms the শব্দ. Passed every gate before CD-138
  PASS  COVERAGE       fires on: an INCOMPLETE admissibility declaration — S12 neither admitted nor excluded with a reason
  PASS  COVERAGE       fires on: a slot excluded with an EMPTY reason — CD-134(c) requires a content reason
  PASS  COVERAGE       fires on: an item that declares NO task — slot id alone says nothing about what it does
  PASS  COVERAGE       fires on: a bank with no admissibility declaration at all
  PASS  COVERAGE       fires on: a register that carries an AUTHORED chapter_authorable — CD-138(f) makes it derived from these very declarations, never authored upstream
  PASS  DOMAIN-RATIO   fires on: an annual paper that is entirely জ্ঞান
  PASS  KEY-RUBRIC     fires on: a descriptive item with no islamic_alignment criterion row
  PASS  KEY-RUBRIC     fires on: an mcq that also carries a rubric — the schema forbids the others
  PASS  KEY-RUBRIC     fires on: a criterion with no descriptor for one of the declared bands
  PASS  SOURCE-TRACE   fires on: a one-word anchor — short enough to match anything
  PASS  COVERAGE       fires on: the bank header states no reason for its target

  PASS  REPETITION     stays quiet on: a `Remember` stem repeated across HW · AS · CT — §5 permits it
  PASS  REPETITION     stays quiet on: a `Remember` stem lifted verbatim from a CT into the annual — §5's listed supersede of MarkLogic §৮'s row 2, and it runs BOTH directions
  PASS  DOMAIN-RATIO   stays quiet on: a bank with NO papers — the ratio is paper level only and must not fire on a pool (§6)
  PASS  DOMAIN-RATIO   stays quiet on: a SINGLE class test skewed all-জ্ঞান — never judged (§5: the ratio is met across the year's tests, not within any one)
  PASS  BLOOM-BAND     stays quiet on: a pool banded flat against the CHAPTER band — §4 says that is correct; only a session-scoped reading drifts low
  PASS  BLOOM-BAND     stays quiet on: a pool at Remember 9/24 = 37.5%, ABOVE REF-06 §3.6's 30% upper bound, with every floor still clear — CD-135: a pool cannot fail a ceiling, because an author declines the surplus and a compliant paper stays constructible. This FAILED until 2026-08-15; it is KEPT AND INVERTED so the symmetric check cannot creep back in unnoticed (CD-122(a)'s device)
  PASS  BLOOM-BAND     stays quiet on: a pool sitting EXACTLY on two floors — Understand 6/24 = 25.0% and Apply 6/24 = 25.0% — the boundary is inclusive, so exactly-at-floor is compliant and must not fire
  PASS  BLOOM-BAND     stays quiet on: EVERY item becomes Remember — every OTHER level drops under its floor at once. THIS WAS A FAIL-SEED UNTIL 2026-08-18; it is KEPT AND INVERTED (CD-171(d)) so the floor cannot creep back unnoticed, exactly as CD-135 kept and inverted the ceiling case. Both bounds are now a PAPER rule
  PASS  BLOOM-BAND     stays quiet on: Analyze at 1 of 24 = 4.2%, under REF-06 §3.6's 10% floor — the case CD-135(h) predicted would become the binding constraint. It did, on পাঠ ১৩ wave 3, and PENDING-P-036 measured the cost: an Analyze floor forbade 28 of 32 items the chapter could genuinely answer. INVERTED
  PASS  COVERAGE       stays quiet on: an admissible slot under the PAPER's per-slot demand — S07 owes 4 and supplies 2. A FAIL-SEED UNTIL 2026-08-18; INVERTED (CD-171(a)(iv)). The paper draws S07's other two items from the chapters that carry them, which is what *demand is paper-level* meant before it was asked of a chapter
  PASS  COVERAGE       stays quiet on: a 12-item pool — under REF-09 §4.3's retired 20-item floor. CD-171(a)(iii) retires the READING, not the number: REF-08 §4.1's 20 sizes a HOMEWORK draw across a year, never a bank's conformance. INVERTED, and the count is still PRINTED so its absence reads as a ruling and not as an oversight
  PASS  COVERAGE       stays quiet on: six items in S10 doing পদ নির্ণয় — the task C5 SELECTED from that slot's three. The gate must fire on the wrong task and stay silent on the right one, or it is not reading the task at all
  PASS  COVERAGE       stays quiet on: a slot declared inadmissible with a content reason and supplying ZERO items — that is the declaration working, not a coverage failure (CD-138(e))
  PASS  ENVELOPE-SYNC  stays quiet on: an export IN SYNC with the bank — ids and payloads agree, array and single/ agree, nothing to say. The gate that fires on a healthy export is the gate an author learns to ignore
  PASS  COVERAGE       stays quiet on: a register carrying a D5 (absent) row for a slot the bank never declares — the chapter owes it NOTHING, not even a content reason. Existence is a CLASS-level fact and admissibility a CHAPTER-level one; requiring a পাঠ to explain why it does not serve a question its paper has never contained would be the two collapsed into one field (Principal, 2026-08-15)
  PASS  COVERAGE       stays quiet on: a register carrying a D6 school's-own row (`BAN-L01`) — skipped entirely. An L-id is in no bank's `admissible_slots`, has no per-slot demand a chapter could owe, and would key as `L01` against a declaration that has never named it
  PASS  COVERAGE       stays quiet on: a bank that declares NOTHING about S14/S15 — neither admitted nor excluded with a reason. Under CD-138(e) that was INCOMPLETE; under CD-147 it is CORRECT, because the bar is categorical and a chapter owes no content reason for a slot its declaration does not reach. This is the base fixture, and it is asserted rather than assumed
  PASS  COVERAGE       stays quiet on: a bank still carrying a CD-139-era content reason for S14 — accepted, not failed. The reason is no longer owed, but a bank that records one is correct history (AGENTS §4), and forcing a same-commit edit of three signed banks to quiet a gate is how a correction becomes a rewrite
  PASS  DIFFICULTY     stays quiet on: a pool at 70% easy — CAN-SUPPLY, not equality, so this passes
  PASS  DIFFICULTY     stays quiet on: a pool that is 67% HARD while still holding easy ≥30% — CD-122: a pool cannot fail a ceiling, because an author can decline to use hard items and a compliant paper stays constructible. This was a SEEDED FAILURE until Q-2 was ruled; it is kept, inverted, so the symmetric check cannot creep back in unnoticed

  PASS  MARK-VALUE     a bank at a class the register has not built is REFUSED BY NAME, not judged and not passed — the vendored table's own refusal, kept through the retirement
  PASS  MARK-VALUE     editing `marks_per_item` in the register alone moves the verdict — the retired tables are gone and nothing else is being read

  PASS  ENVELOPE-SYNC  fires on: a wrapper whose item_count misdescribes its own items — the Hub rejects that WHOLE and imports nothing, so a wrapper that lies about itself is worse than a missing one: it looks importable
  PASS  ENVELOPE-SYNC  fires on: a wrapper BEHIND the bank while array and single/ are current — the third artifact drifts on its own, and it is internally consistent while doing so
  PASS  ENVELOPE-SYNC  fires on: a wrapper carrying an item the bank no longer has — the silent half of the wave-2 defect, now in the file the Hub actually reads
  PASS  ENVELOPE-SYNC  fires on: a wrapper describing a DIFFERENT bank than the one on disk. The contract does NOT recompute digest at import — it is an audit field — so nothing downstream catches this, which is precisely why it is caught here
  PASS  ENVELOPE-SYNC  stays quiet on: no `.batch.json` at all — an unrun step is not drift, and failing there would fire on every bank exported before v1.1 (SOURCE_POLICY §7.17)

  PASS  CD-138(b)      every marker-bearing prose field in the register rewritten to noise — verdict byte-identical, because COVERAGE reads DECLARED fields and no marker string

  PASS  DECLARE        a bank declaring নির্মাণাধীন is held OUT of the controls, and the reason names the resume point
  PASS  DECLARE        stays quiet on: a finished, unmarked, §4-shaped bank — it IS a control
  PASS  DECLARE        a bare `নির্মাণাধীন` with no resume tail is REFUSED, not silently skipped
  PASS  DECLARE        THE MARKER IS NOT A WAIVER — every gate still runs on a marked bank and TOPIC-NUMBER still reports it red
  PASS  DECLARE        and it changes no verdict: marked and unmarked copies of the same bank give byte-identical gate output
  PASS  DECLARE        an `অবস্থা` carrying some other word is REFUSED — §7.9 defines exactly one marker

  PASS  baseline: a 44-item bank at exactly +2 on every floor is SIGNABLE
  PASS  PAPER-LEVEL       fires on: a plan ADMITTING S15 — CD-147 puts S14/S15 in the paper/exam pipeline for EVERY chapter, and PLAN refuses it in its own voice rather than leaning on COVERAGE: the two gates ask different questions and a bank is offered as FINISHED on this one's verdict
  PASS  TASK-MISSING      fires on: an item that declares no task at all
  PASS  OFF-CHOICE        fires on: an S10 item doing ক্রিয়ার কাল — admitted at the slot, NOT selected at C5
  PASS  COMPOSITE-HALF    fires on: an S12 item that breaks the যুক্তবর্ণ and never forms the শব্দ
  PASS  P-037             fires on: a teacher-keyed item typed `mcq` — the interim rule admits short_answer and descriptive only
  PASS  DUP-EXACT         fires on: two items in one slot carrying the SAME stem verbatim
  PASS  DUP-NEAR          fires on: two stems 95%+ identical without being identical
  PASS  NEGATIVE  ZERO-FLOOR-POOL   stays quiet on: a 12-item pool — under CD-141(g)'s retired 40-item minimum, under REF-09 §4.3's retired 20, and short of every REF-06 §3.6 floor at once. THE BRIEF'S NAMED CASE: it must PASS
  PASS  NEGATIVE  EXACTLY-ON-FLOOR  stays quiet on: a level landing EXACTLY on its floor — FAILED before CD-171 as *landing exactly on a floor is a DEFECT, not a pass*
  PASS  NEGATIVE  MARGIN            stays quiet on: margin +1, one item short of the retired rule
  PASS  NEGATIVE  DEMAND            stays quiet on: an admissible slot six items under the PAPER's per-slot demand — CD-138(g) is retired at pool level and the paper draws the rest from other chapters
  PASS  DUP-BORDERLINE   stays quiet on: a pair in the 85–95% band — REPORTED for the Hub's subject expert, never failed and never silently dropped
  PASS  LIVE-CONTROL     stays quiet on: the signed 106-item পাঠ ১৩ — every task declared and admitted, no paper-level slot claimed, and S12's per-word drill REPORTED as borderline rather than failed. (The margin and full-demand clauses this line used to cite are retired — CD-171(d).)

SELFTEST RESULT: PASS (29 seeded failures + 20 negatives + 6 CD-055 declaration cases + 1 baseline, across all 14 gates; PLAN adds 7 seeds + 6 negatives + 1 baseline on its own 44-item fixture — FOUR of those negatives are CD-171(d)'s inverted seeds, and they are the load-bearing half: a retired rule leaves no failure to seed, so the only available proof is that the states it used to fail on now pass)

--- CD-150 · paper-level re-keyed to (subject, class, slot) -------------------
  PASS  ENUMERATION  29 (subject, class, slot) assertions — 11 barred, 18 chapter-lane, boundary classes asserted on BOTH sides
  PASS  REGRESSION   BAN-S05 (বহুনির্বাচনি) and BAN-S13 (এক কথায় প্রকাশ) remain chapter-admissible at EVERY class — the collision a flat set of slot shorts would have caused, asserted rather than argued
  PASS  FAIL-SEED  C5 bank with an item IN S05      fires: the unseen passage is textbook-EXTERNAL — no chapter can source it, so an item there is categorical, not a mis-declaration
  PASS  FAIL-SEED  C5 bank with an item IN S13      fires: চিঠি at C5
  PASS  FAIL-SEED  C5 bank with an item IN S14      fires: রচনা at C5
  PASS  FAIL-SEED  C5 header ADMITTING S13          fires: admitting is a failure even with no item in it
  PASS  NEGATIVE   C5 bank omitting all three       quiet: OWES NO REASON — the obligation CD-150 removes. A gate that only gained the FAIL would redden every conformant ENG bank on its first run
  PASS  NEGATIVE   C3 bank with an item IN S13      quiet: C3's S13 is DIALOGUE, a D4 substitute — chapter lane, and the bar must not reach it
  PASS  NEGATIVE   C3 bank with an item IN S14      quiet: C3's S14 is the guided paragraph — chapter lane
  PASS  MARK-VALUE   ENG-S10 at 0.5 is quiet and the same item at 1 FAILs — the half mark rides through as DATA, and no gate code was changed to admit it

--- UNSELECTED · alternative rows the source declined to narrow --------------
  PASS  COVERAGE   UNSELECTED, item does form A           quiet: admitted — the register narrowed to nothing, so this is not off-choice
  PASS  COVERAGE   UNSELECTED, item does form B           quiet: the OTHER form, equally admitted — this is the half a naive fix would have broken
  PASS  COVERAGE   UNSELECTED, item does neither          fires: UNSELECTED widens the set to all of admitted_set, NOT to anything at all
  PASS  COVERAGE   SELECTED, item does the selection      quiet: ordinary selected behaviour, unchanged
  PASS  COVERAGE   SELECTED, item does the OTHER admitted form fires: THE REGRESSION SEED — a row with a real selection still FAILs off-choice items; the UNSELECTED branch must not have turned every alternative row into a free-for-all
  PASS  PLAN       UNSELECTED, item does form B           quiet: admitted at PLAN too
  PASS  PLAN       SELECTED, item does the OTHER form     fires: PLAN's own off-choice check survives the change

--- TAUGHT SET · the বিরামচিহ্ন a class is taught ---------------------------
  PASS  TAUGHT SET  a সেমিকোলন in the only accepted answer       fires: the poem's own mark is still a mark the class is not taught
  PASS  TAUGHT SET  a ড্যাশ in the only accepted answer          fires: same, on the other barred mark
  PASS  TAUGHT SET  the four admitted marks, all at once         quiet: কমা · উদ্ধরণ · দাঁড়ি · প্রশ্নচিহ্ন · বিস্ময়চিহ্ন together — every one admitted, so silence
  PASS  TAUGHT SET  NO `taught_set` on the row at all            fires: ABSENCE IS NOT PERMISSION (CD-165) — a class whose marks are undeclared cannot have its S11 items checked, so none of them passes
  PASS  TAUGHT SET  a note SAYING the barred mark is not needed  quiet: DRAFT-1 REGRESSION SEED — prose about a mark is not a mark, and a check that cannot tell REQUIRES from FORBIDS fails an item for getting it right
  PASS  TAUGHT SET  variant 1 barred, variant 2 clean            quiet + REPORTS: DRAFT-2 REGRESSION SEED — a student writing variant 2 is right, so nothing is REQUIRED outside the set; PASSES and is REPORTED, because accepted[0] is what a marker reads first

  PASS  CD-172  NEGATIVE  the two CD-172 ids, each requiring a BARRED mark quiet, 2 excluded id(s) REPORTED BY NAME: NAMED, JUSTIFIED, ID-KEYED — the defect is real and is REPORTED in full; what it does not do is fail CD-153's push condition, because CD-169 rules the items RETAINED and CD-170 bars the repo from fixing them
  PASS  CD-172  FAIL-SEED a THIRD id in the SAME slot requiring ড্যাশ      fires, 2 excluded id(s) REPORTED BY NAME: THE LOAD-BEARING SEED — same bank, same slot, same barred mark, id not on the list. It MUST still FAIL, or the exclusion is check-level in disguise and every future taught-set defect went invisible to buy two items' silence
  PASS  CD-172  ROSTER    prints every run, naming both ids and which EXERCISED the exclusion — an exclusion that prints only when used is invisible on the run it stops being needed
  PASS  CD-172  DISCHARGE prints UNEXERCISED once the ids stop needing the exclusion — the expiry condition announces itself on the run it becomes true, rather than waiting for somebody to re-read the row (CD-160: a list drifts)

SUITE: 24 gates (11 carry a QUESTION_POLICY §6 row, 14 a QUESTION_BANK_POLICY §5 row, 4 both, 3 from a decision row with no § of its own — CD-131)
CONSUMPTION EXCLUSIONS IN FORCE: 1 — BAN-C5-chapter 12 (CD-127, canon/_wip/c5-bangla/EXCLUDED_paath_12.md)

BANK: workstreams/question-banks/banks/C5_BAN_U18_QuestionBank_v1.json   [shape: qp6]
  N/A   POOL-MEMBERSHIP    reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   ZERO-OVERLAP       reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  PASS  MARK-VALUE         [§5 · QB-CR-003 + §6.1]
  PASS  SOURCE-TRACE       [§5 + §6.2]
  PASS  SCRIPT-GUARD       [§5 + §6.3]
  PASS  REF19-SLUG         [§6.4]
  PASS  TOPIC-NUMBER       [§5 · QB-CR-008 + §6.5]
  PASS  KEY-RUBRIC         [§6.6]
  PASS  BLOOM-BAND         [§6.7 · CD-121 · CD-171 (report)]
  PASS  DIFFICULTY         [§6.8 · CD-122]
  PASS  REPETITION         [§6.9]
  PASS  COVERAGE           [§6.10 · CD-138]
  PASS  DOMAIN-RATIO       [§6.11 (per-pool RETIRED)]
  N/A   ANSWER-SHAPE       reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   RUBRIC-SPECIFICITY reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   FLAG-TRACE         reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   QUOTE-VERBATIM     reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   HONORIFIC          reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   AS-MIX             reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   NUMERALS           reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  N/A   CEILING            reads QUESTION_BANK_POLICY §5's shape; this bank is qp6
  PASS  SOURCE-EXCLUSION   [CD-127(b) · CD-131]
  PASS  PLAN               [AGENTS §6 · Principal 2026-08-15 · CD-171(d)]
  PASS  ENVELOPE-SYNC      [AGENTS §11 · Principal 2026-08-15]
  REPORT  MARK-VALUE         79 items checked against the SLOT REGISTER's BAN C5 rows (CD-138 — the vendored mark table is retired)
  REPORT  SOURCE-TRACE       79 anchors resolved against the extraction
  REPORT  SCRIPT-GUARD       tier-3 WATCH counter (em-dash/ellipsis): 50; tier-2 GREY in metadata: 0
  REPORT  REF19-SLUG         121 REF-19 slugs loaded from the LOCKED artifact
  REPORT  TOPIC-NUMBER       11 charted topic numbers loaded
  REPORT  KEY-RUBRIC         key/rubric checked per type on 79 items
  REPORT  BLOOM-BAND         POOL check is REPORT ONLY (CD-171(d), superseding CD-135; CD-121/UD-23 for the axis, unamended — MarkLogic §৩ is the PAPER's axis and is not read here). NEITHER bound binds a pool. Both bind the PAPER, which composes across chapters, and no single chapter owes a paper's distribution. The `bloom_level` TAG is still required on every item and an unknown level still FAILs — recorded, not rationed.
  REPORT  BLOOM-BAND         levels below REF-06 §3.6's C3–5 indicative lower bound — PRINTED, NOT FAILED, and §3.6 calls its own ranges *only indicative*: Analyze 6/79=7.6% vs 10%
  REPORT  BLOOM-BAND         per-level counts against REF-06 §3.6 C3–5 floors (printed every run, pass or fail): Remember 32/79=40.5% vs floor 20% · Understand 20/79=25.3% vs floor 25% · Apply 20/79=25.3% vs floor 25% · Analyze 6/79=7.6% vs floor 10% ✗ · Evaluate 0/79=0.0% vs floor 0% · Create 1/79=1.3% vs floor 0%
  REPORT  BLOOM-BAND         levels at 0 against a 0% floor — nothing is required, but §4 requires the bank header to state these as a CONTENT fact rather than leave them silent (CD-135(d)): Evaluate
  REPORT  BLOOM-BAND         read at CHAPTER scope, never per session — §4's D-050 paragraph
  REPORT  DIFFICULTY         pool: easy 53.2% · medium 39.2% · hard 7.6%  (easy floor only — CD-122; the hard ≤25% ceiling is a PAPER-level test and is not applied to a pool)
  REPORT  REPETITION         0 `Remember` repetition(s) permitted by §5 — expected, not an error
  REPORT  COVERAGE           pool holds 79 item(s) — NO MINIMUM AND NO CEILING (CD-171(a)). The bound is the chapter's content under §4's near-duplicate ban
  REPORT  COVERAGE           slot(s) supplying under the PAPER's per-slot demand — PRINTED, NOT FAILED (CD-171(a)(iv)): the demand is the paper's and the paper composes across chapters. S12 4/5
  REPORT  COVERAGE           coverage read against THE REGISTER (CD-138 — this replaces the header-stated target, §4's own successor clause): 15 slot(s) for BAN C5, 13 declared admissible, 0 excluded with a content reason (CD-138(e)), 2 paper-level and outside the declaration entirely (CD-147)
  REPORT  COVERAGE           per-slot demand is PAPER-LEVEL and undivided (CD-138(d)) and is asked of the PAPER, never of a chapter (CD-171(a)(iv)). `PENDING-P-036` is CLOSED-MOOT with CD-171: there is no pool floor left for its `min()` to take a minimum of
  REPORT  DOMAIN-RATIO       no papers in this bank — domain ratio is PAPER LEVEL ONLY (§6) and is not applied to a pool. Not judged; not assumed clean.
  REPORT  SOURCE-EXCLUSION   consumption exclusions in force: BAN-C5-chapter 12 (CD-127)
  REPORT  PLAN               P-037: 17 item(s) declare a teacher-supplied key in their own model_note, all on admitted types
  REPORT  ENVELOPE-SYNC      no `.batch.json` beside this export — contract v1.1's wrapper has not been built for this bank. REPORTED, not failed: an unrun step is not drift (SOURCE_POLICY §7.17)
  REPORT  ENVELOPE-SYNC      export in sync: 0 envelope(s) == 79 bank item(s), ids and payloads, array and single/ (digest 10fe9d391319)
RESULT: CLEAN (0 failures)
