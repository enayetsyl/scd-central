# CORRECTIONS.md — question-banks ledger (append-only, AGENTS.md §6)

<!-- CD-124: the prefix is what rows in this file ALREADY mint; the lane identifies this ledger. Declared now, renumbered per lane at close — no row is renumbered by this line. -->
<!-- ledger-prefix: QB-CR -->
<!-- ledger-lane: question-banks-corrections -->

Log every teacher/Principal correction same-session. Re-read before drafting.
3+ occurrences → PATTERN → propose promotion to an executing gate.
A fix on one artifact must be checked across its siblings same-session.

| ID | Date | Correction | Source | Status |
|---|---|---|---|---|
| QB-CR-010 | 2026-08-14 | **`SOURCE-EXCLUSION` counted a CITATION of its own declaration as a MINT — found by the gate's own first live run, one commit after it shipped.** `06811f7` printed **`CONSUMPTION EXCLUSIONS IN FORCE: 2`** where there is exactly one exclusion: the loader matched the declaration string inside **CD-131's decision row**, which quotes the form to document it. **Harmless as it stood — same subject, same chapter, same CD row — and a real defect:** a `SOURCE_POLICY` clause, a session log or a CD row that *shows* the form would mint a **phantom exclusion**, and a genuine exclusion could be created by someone merely describing one. **This is AGENTS.md §5.1** — *a gate must not make naming the defect unwriteable* — and it is the **third** site of that rule (§7.16's Assamese gate, CD-085's REF-CITE census, CD-089's PLACEHOLDER; now this). **`ledger_check.py` had already solved the identical mint-vs-cite problem** by anchoring on the first cell of a table row, and that solution was not carried across when this loader was written. | Agent, running the suite after committing `06811f7` | **CLOSED 2026-08-14** — the declaration must now be **the WHOLE LINE** (a standalone `<!-- … -->`), and lines inside a fenced block are skipped so P-035's proposed policy clause can print its own convention. **Three seeded cases, both directions**: the standalone comment is read · the same string in a table row is not · the same string in a fence is not. Live count back to **1**. |
| QB-CR-001 | 2026-08-09 | **The v0.1 draft's §3 "pool rides on `paper_role`" is wrong at source.** `paper_role` is a closed enum (`mcq`/`short`/`structured`/`creative`) already meaning the REF-09 paper-section family, and the payload schema is `additionalProperties: false`. Corrected in v1.0: pool lives in the bank's `pool_index`; gap raised as UP-002. | Agent, verifying the LOCKED schemas at source before writing v1.0 | **PROMOTED** — the POOL-MEMBERSHIP gate reads `pool_index`, and the payload closure is enforced by harness L2 |
| QB-CR-002 | 2026-08-09 | **A non-REF-2 personal name is in the accepted Ch21 class test.** `workstreams/class-tests/_wip/C5_Bangla_ClassTest_Ch21.md` Q4 uses **আসিফ**, carried from the NCTB অনুশীলনী ৪; আসিফ is **not** in `canon/names/REF-2_Content_Register.md`, and QuestionPolicy §৯ requires every name in student-facing text to come from the class pool. Bank items use **সাবিত** (REF-2 C5 male #3) instead. **The accepted CT is NOT edited** — it is a Principal-accepted artifact and any change is his call (CD-023 pattern). Cross-logged to `workstreams/class-tests/CORRECTIONS.md`. | Agent, during পাঠ ২১ wave-1 authoring | **CLOSED 2026-08-09** → QB-CR-005 |
| QB-CR-003 | 2026-08-09 | **S11 per-item marks: spine and accepted CT disagree in form, not in total.** `MarkLogic_BAN_Spine.md` `BAN-S11` reads *৫টি · 1×5 = 5*; the accepted Ch21 CT renders বিরামচিহ্ন as a single 5-mark passage. Both are 5 marks. The bank models it as one `fill_blank` item with **5 blanks × 1 mark**, so the spine's per-item value and the CT's presentation both hold, and the harness L4 per-blank sum check passes. | Agent, reconciling spine against the accepted CT | **PROMOTED** — MARK-VALUE gate checks per-item marks against the spine, and L4 checks the blank sum |
| QB-CR-004 | 2026-08-09 | **`TOP-BAN-C5-02` is used for the S03 near-homophone sentence items on a stated default.** পাঠ ২১'s attested topic tags are `-07`, `-01`, `-11` (`PROJECT03_TODO.md` §C and the P04 Pools row); `-02` is attested as *sentence/রচনা* from the U20 Chapter Plan but **not** attested for পাঠ ২১. The per-subject revision chart that defines the `TOP-` numbers is not in this repo, so the number is **not invented and not assumed** — it is used on a default and queued. | Agent, verifying topic tags at source | **CLOSED 2026-08-09 → CD-043** — verified at source; `-02` CONFIRMED |
| QB-CR-005 | 2026-08-09 | **Principal ruling on QB-CR-002 / PENDING-P-006 — narrower than a carve-out, and the narrowness is the point.** The accepted Ch21 class test **stays untouched** and **আসিফ is grandfathered in that one historical paper only**. This is **not** a general exemption for names quoted from NCTB exercises: **every new item must use a REF-2 C5-pool name**, and **সাবিত** in the পাঠ ২১ bank is **confirmed correct**. The practical consequence for authoring: when an NCTB exercise supplies a personal name, the name is replaced from REF-2 while the exercise's structure is kept — the name is not part of the KEEP-AS-IS obligation, the text of the ভাষণ is. | Principal ruling 2026-08-09 | **PROMOTED** — REF-2 pool membership is enforced by authoring rule; the ruling is recorded here and at CD-042 |
| QB-CR-006 | 2026-08-09 | **PENDING-P-005 reclassified OPEN → FLAGGED (file-owed).** `TOP-BAN-C5-02` on the 8 S03 items stays **FLAGGED, not confirmed**. The Principal will stage the revision chart that defines the `TOP-BAN-C5` numbers into `_inbox/`; until it is verified at source the tag **must not be changed by any agent** and **does not block** wave-1 promotion or wave-2 authoring. Close only on verification against that file. | Principal ruling 2026-08-09 | **FLAGGED** — carried in the promoted bank's own `flags` block so a downstream reader sees it without reading this ledger |
| QB-CR-007 | 2026-08-09 | **PENDING-P-005 close instruction received and NOT executed — two of its three citations do not resolve in this repo.** Ruling as given: REF-19 v1.10 is staged and verified, slugs-only with no `##` numbering; REF-19 slugs plus the D-PROJ04-attested numbering (`-02` = বাক্য-রচনা per **D-PROJ04-003/011**) are the interim numbering authority; `TOP-BAN-C5-02` is CONFIRMED under it; commit REF-19 into `canon/`. **Checked at source before acting (AGENTS.md §4):** **(a) `_inbox/REF-19 v1.10` is ABSENT** — not in `_inbox/`, not anywhere in the repo under any REF-19 / topic / registry / slug filename, and the only files modified in the preceding two hours are ones this session wrote. This is **CD-026 recurring**, and precisely the failure `canon/sources/SOURCE_POLICY.md` §2.1 was written to warn about: `_inbox/` is gitignored and therefore per-machine, so a file staged elsewhere never reaches this workspace. **(b) `D-PROJ04-003` and `D-PROJ04-011` do not exist here** — the P04 decision register was never imported. Exactly five D-PROJ04 numbers are cited anywhere in the repo (001, 002, 005, 014, 015), all as citations inside the vendored schemas and `VENDOR.md`, none as bodies; 003 and 011 are cited nowhere at all. Under AGENTS.md §4 these are **phantom citations, and the Principal having supplied them does not change that** (the CD-026 / CD-033 precedent). **(c) What DOES corroborate, and its limit:** the slugs-only half of the ruling checks out from a derived copy — `tools/hub-export/validate_import.py`'s inlined `REF19_SLUGS_DEFAULT`, auto-extracted from LOCKED_REF-19 v1.10, holds **121 slugs (24 BAN) and not one carries a numeric suffix** — and `LOCKED_QuestionPayload_Schema_v1.json` confirms `topic_tag` is **pattern-only**, its numbers coming from *"the per-subject revision chart + REF-19, not a registry in this schema (QDN-02)"*, citing REF-07 §3.5. **But CD-011's standing rule is that such a registry is written from the actual artifact, never from a summary or a derived copy, and a disagreement between sources goes to the Principal rather than being merged.** A canon file reconstructed from a harness constant would be exactly the phantom content CD-026 refused to produce. **Nothing was closed, nothing was committed to `canon/`, and `TOP-BAN-C5-02` was not changed.** | Principal instruction 2026-08-09; verified absent at source | **CLOSED 2026-08-09 → CD-043** — both files staged on this machine, imported byte-identical, and all four conditions verified PASS |
| QB-CR-008 | 2026-08-09 | **A mis-tag in the PROMOTED bank, found by checking the tags the ruling did NOT ask about.** PENDING-P-005 covered the 8 S03 items only. With `PROJECT04_DECISIONS.md` finally in the repo, all four of the bank's tags became checkable for the first time: `-01` শব্দার্থ **correct**, `-02` বাক্য-রচনা **correct**, `-07` `BAN-INFOTEXT` **correct** (D-PROJ04-013) — and **`-11` WRONG**. `D-PROJ04-011` attests **`TOP-BAN-C5-11` = মূল্যবোধ / মুক্ত-চিন্তা**, a values / free-thinking strand that D-PROJ04-003 further marks PROTECTED and rubric-only; the bank's `QP-BAN-C5-U21-Q52` is the **বিরামচিহ্ন** fill_blank. **Origin of the error: it was read off the MarkLogic spine slot `S11 = বিরামচিহ্ন`. The S-slot numbers and the TOP- topic numbers are unrelated schemes that collide at 11 by coincidence** — and unlike `-02`, this inference was never queued, so nothing flagged it. **No number is attested for বিরামচিহ্ন anywhere in the register, so no substitution was made and the tag was not guessed.** Recommendation to the Principal: **mint `TOP-BAN-C5-13`** for বিরামচিহ্ন/যতিচিহ্ন rather than folding it into `-02`, because the C5 spine keeps `S03 বাক্য গঠন` and `S11 বিরামচিহ্ন` as separate mark slots and merging them would erase a distinction canon makes. | Agent, checking beyond the ruling's scope after the P04 register import | **CLOSED 2026-08-09 → CD-044** — `TOP-BAN-C5-13` minted for বিরামচিহ্ন, charted in `canon/topics/TOPIC_NUMBERS.md`, and **PROMOTED to an executing gate (TOPIC-NUMBER)**. **Standing lesson kept: a tag inferred from a same-numbered field in a different scheme must be queued like any other unverified value.** One inference was queued and survived; the other was not and did not. |
| QB-CR-009 | 2026-08-14 | **U14 (কুপোকাত) is Drama `TOP-BAN-C5-09`, not Story `TOP-BAN-C5-06` — the bank's tag is wrong and is NOT corrected in place.** The contested row carried since D-PROJ04-003 is ruled: `REF-03 §4.5/§5.5` maps U14 to Drama; the bank tagged it Story and flagged the mismatch itself. **The REF wins on the authority chain** — REF-03 is a Project-00 subject-spine playbook, D-PROJ04-003 is a workstream-local bank tag — **and the source agrees rather than merely permitting**: `canon/marklogic/C5_Bangla_Source_13-23.md`'s এক নজরে table gives পাঠ ১৪'s ধরন as **নাটক**, and REF-19's C5 Bangla row reads `BAN-DRAMA` — *নাটিকা U14*. **No REF-03 supersede is owed:** the re-home was rejected, so REF-03 already says what the ruling says. **Execution is append-only and deferred:** the affected items are **not re-tagged in this session** and no bank file was touched — a silent re-tag would destroy the evidence that the bank was ever wrong, which is what this ledger exists to preserve. The re-tag is authored as a dated correction against the affected items when the U14 bank is next opened, citing this row. `-06` (গল্প) keeps its chart row and stays correct for the chapters that really are stories (REF-19 C5: U06, U09, U12). | Principal ruling 2026-08-14 (unification session 1 closing paste-back, ruling 5) | **RULED — execution owed.** Charted at `canon/topics/TOPIC_NUMBERS.md`; U14 items still carry `-06` on disk and must be corrected before any U14 bank is promoted. |
| **QB-CR-011 · PATTERN CANDIDATE** | 2026-08-15 | **A SPINE SLOT READ AS CARRYING A PROPERTY IT DOES NOT CARRY — second instance, promoted to a named candidate by the Principal.** *(Next free verified at source: this ledger defines through `QB-CR-010`; no `QB-CR-011` token existed anywhere in the repo.)* **What happened.** Sizing wave 3, the agent derived every item's `bloom_level` from its **slot identity** — S04 শূন্যস্থান → `Remember`, S05 বহুনির্বাচনি → `Remember`, S12 যুক্তবর্ণ → `Remember` — and projected the pool from that. **Bloom is a property of the item's COGNITIVE DEMAND. `MarkLogic §৩` assigns domains to a PAPER; `QUESTION_POLICY` §3 row 10 maps domain↔Bloom. Neither fixes a slot's level.** **The spine says so in its own words and the agent read past it:** `MarkLogic_BAN_Spine.md` `BAN-S05` — ***"বহুনির্বাচনি হলো উত্তর দেওয়ার একটা পদ্ধতি, কোনো দক্ষতা নয়।"*** A slot the spine explicitly calls **not a skill** cannot carry a cognitive level. **Instance 1 = `CR-007`** (`canon/_wip/c5-math/CORRECTIONS.md`, 2026-08-11): the MATH spine's eleven slots are **পরীক্ষার প্রশ্ন-ধরন**, and অধ্যায় ৩·৪'s slot tables wrote them as **অধ্যায়-বিষয়** — the whole list shifted one cell from `S05` and `S01`–`S04`'s names were invented. **Instance 2 = this row.** Different repo lane, different property, **same shape: the slot's NAME was read as a claim about the item.** **Why it was hard to see, recorded because it is the transferable part.** The slot-derived tagging was **right four times out of six** — it fails where the slot's name is a **format** (`বহুনির্বাচনি`) or a **procedure** (`যুক্তবর্ণ ভেঙে`), and happens to be right where the name describes a recall task. **A heuristic that is usually right for the wrong reason is harder to catch than one that is always wrong.** **Cost, measured.** Under slot-derived tagging, absorbing all released items priced at **+31 extra items, pool 99**; under demand-derived tagging, **+11 extra, pool 79**. **Nearly 3×** — the error would very likely have ended in *"the chapter cannot support it."* **Corrected before anything was authored**, by a four-item probe (`_wip/U13_BLOOM_PROBE_2026-08-15.md`) tagged on demand with per-item reasoning. **Two of six slots moved: S05 carries BOTH `Remember` and `Understand`; S12 read as `Apply`.** **A third instance promotes this to a PATTERN and a gate proposal.** No gate is proposed now: a gate cannot read cognitive demand, which is why `QUESTION_POLICY` §6's human review row is where this is caught. **What IS executable is the tagging rule the Principal minted with it — under floors, tag DOWN when uncertain** (applied to all 3 S12 items, authored `Remember` though the probe read `Apply`). | Principal correction 2026-08-15; `MarkLogic_BAN_Spine.md` BAN-S05; `CR-007`; CD-135; CD-088 (the PATTERN-promotion form) | **CANDIDATE — 2 of 3 instances. Probe filed; wave 3 authored on demand-derived tagging.** |
| **QB-CR-012 · PATTERN** | 2026-08-15 | **`QB-CR-011` IS PROMOTED TO PATTERN AT FOUR INSTANCES — and the third and fourth both happened while the ruling that names the pattern was being drafted (Principal direction 2026-08-15; AGENTS.md §6; CD-088's promotion form).** *(Next free verified at source: this ledger defines through `QB-CR-011`; no `QB-CR-012` token existed anywhere in the repo outside this row. `QB-CR-011`'s own row is UNEDITED — append-only, AGENTS.md §4; its status cell still reads CANDIDATE and that is correct history.)* **THE PATTERN, in the words the candidate row minted it in: *a spine slot read as carrying a property it does not carry* — more generally, READING A SURFACE INSTEAD OF THE THING UNDERNEATH.** **(a) The four instances.** **1 — `CR-007`** (`canon/_wip/c5-math/CORRECTIONS.md`, 2026-08-11): the MATH spine's slots are পরীক্ষার প্রশ্ন-ধরন, written into অধ্যায় ৩·৪'s tables as অধ্যায়-বিষয়; the list shifted one cell and four slot names were invented. **2 — `QB-CR-011`** (2026-08-15): `bloom_level` derived from slot identity, on a spine that says in its own words *"বহুনির্বাচনি হলো উত্তর দেওয়ার একটা পদ্ধতি, কোনো দক্ষতা নয়"*. **3 — CD-138(1) as first drafted** (2026-08-15, corrected before it shipped): `task_mode` derived from whether the spine header carries *"যেকোনো একটা"*. **`ENG-S01`, `ENG-S06` and `ENG-S13` state alternatives with অথবা / বা and no marker** — all three would have been encoded COMPOSITE — while **`MATH-S01` carries *"যেকোনো C4 অধ্যায়"*** meaning a source range and the MATH checklist carries the same token naming a thing that must never appear. **The marker is a surface; the mode is the thing underneath.** **4 — the CD-136 scope misread** (2026-08-15, advisor error 13): CD-136's scope was taken from a one-line SUMMARY of the row in a handoff §2 rather than from the row, and a conflict was reported where none existed — CD-136(c) rules S14/S15 explicitly. **The summary is a surface; the row is the thing underneath.** **(b) Instance 3 and 4 are the ones that matter for promotion, because of WHEN they happened.** Both occurred **inside the session that was drafting the ruling that names this pattern**, by the party that had just written the pattern's own description. **A pattern is not promoted because it is rare; it is promoted because knowing it does not prevent it.** **(c) NO GATE IS PROPOSED, and the reason is unchanged from the candidate row.** A gate cannot read cognitive demand (instance 2), cannot know which cell of a table a name belongs in (instance 1), and cannot tell a summary from its source (instance 4). **What IS executable is instance 3's residue, and it is ruled: CD-138(b) forbids any gate deriving mode, membership or cardinality from a marker string, and CD-138(e) forbids inferring admissibility from content.** Both are seeded, including a marker-edit seed whose whole job is to prove the gate cannot see the surface. **A pattern that cannot be gated is closed by writing the specific inference out of the specific gate — one at a time, each in the row that builds it.** **(d) The behavioural residue, for the advisor lane:** **do not cite a §-number, a CD row or a policy scope you have not read AT SOURCE in this conversation** — a handoff's summary of a row is not the row. Eleven advisor errors were on the chain's ledger at session start; this row records **12 and 13**. | Principal direction 2026-08-15; `QB-CR-011` (unedited); `CR-007`; CD-088 (promotion form); CD-138(b), CD-138(e); CD-136(c); CD-139; AGENTS.md §4, §6 | **PATTERN — promoted at 4 instances. No gate proposed; the executable residue is ruled at CD-138(b)/(e) and seeded there.** |
| QB-CR-013 | 2026-08-15 | **A PRINCIPAL ACT WAS RECORDED BEFORE IT OCCURRED — the agent wrote a Subject Lead signature into `banks/C5_BAN_U13_QuestionBank_v1.json` on the strength of a MENU SELECTION about how to record one.** **What happened.** The Principal replied *"approve"* to a read-only render task. The word was ambiguous — it could mean *commit the sheet* or *this is the §6 pass* — so the agent asked, correctly, via a multiple-choice question, and the answer came back **"Both, in two commits"**. **The agent then treated that answer as the sign-off itself** and committed `header.subject_lead_review` reading *"PASSED — Principal as Subject Lead, 2026-08-15"*, **with no verdict from the Principal anywhere in it.** The actual verdict — *"i checked the questions and they are ok."* — arrived one message later. **The defect, stated so it is recognisable next time.** *An answer about the MECHANICS of recording an act is not the act.* The question asked was **where the signature should go**; it was read as **whether the signature was given**. The two are one word apart in a menu and a full governance step apart in fact, and **the menu form is what made them look alike** — a selection returns a token, not a sentence, and a token carries no evidence of what was actually reviewed. **Why nothing caught it.** No gate can: AGENTS §2 (*the agent never self-approves*) is protocol addressed to a person, and CD-136(g) deliberately puts this enforcement at the human review gate because *a gate cannot see the thing it judges*. The suite ran CLEAN across the whole episode — **correctly**, since nothing structural was wrong. **A green suite over a signature nobody gave is exactly the failure mode CD-136(e) predicted.** **Cost, and what limited it.** Nothing was pushed, so the damage was one local commit. The Principal caught it by stating an expected HEAD in his next brief; the agent verified, found the mismatch, and stopped — **the stop-on-mismatch rule did the work no gate could.** **FIRST INSTANCE, agent-side.** Filed before the teacher-workflow autonomy expands, because the same shape scales badly: a teacher's *"ok"* to a question about process would be read as a content approval by the same mistake. | Principal, 2026-08-15, by stating an expected HEAD that did not match | **CLOSED 2026-08-15** — the premature commit was dropped by `git reset` (unpushed; none of its text reused) and the signature **authored fresh** carrying the Principal's words verbatim, a date, the review sheet as evidence, and a **content hash pinning it to the exact 110 items reviewed** so no later edit rides under it. **The standing rule this mints: a signature row quotes the signer. A row that cannot quote its signer is not a signature — it is the agent's summary of one, and no artifact may carry it.** No gate proposed: this is AGENTS §2's territory and a gate that could judge it would have to read intent. |
| QB-CR-014 | 2026-08-16 | **পাঠ ১৩'s EIGHT `S11` বিরামচিহ্ন ITEMS CARRIED `TOP-BAN-C5-02`; `TOPIC_NUMBERS.md`'s OWN MINTING RATIONALE RULES `-13` CORRECT — found by authoring the NEXT chapter, not by re-reading this one.** *(Next free verified at source before writing: this ledger defines through `QB-CR-013`; no `QB-CR-014` token existed anywhere in the repo outside this row.)* **(a) HOW IT SURFACED.** The পাঠ ১৪ session (2026-08-16, `1b07abc`) tagged its six `S11` items `TOP-BAN-C5-13` and, in doing so, noticed পাঠ ১৩'s eight carry `-02`. **Two signed-and-CLEAN banks disagreed about the same slot at the same class**, and the disagreement was reported rather than acted on, পাঠ ১৩ being outside that session's chapter scope. **(b) WHY `-13` IS THE RIGHT TAG, and the authority is the chart's own reasoning rather than a preference.** `canon/topics/TOPIC_NUMBERS.md` carries a section headed *"Why `-13` was minted rather than folded into `-02`"*, recording the Principal's ruling of 2026-08-09: ***"`MarkLogic_BAN_Spine.md` keeps `S03 বাক্য গঠন` and `S11 বিরামচিহ্ন` as separate mark slots at C5"***, and folding punctuation into `-02` (বাক্য-রচনা) ***"would erase a distinction canon makes about what is being assessed."*** `-13` exists **because** this exact fold is wrong. পাঠ ১৩'s `-02` on `S11` is that fold, committed in the artifact. **(c) NO GATE COULD SEE IT, AND THAT IS THE ROW'S REAL CONTENT.** `TOPIC-NUMBER` checks that a `topic_tag` **resolves to a charted row** (CD-044, built on `QB-CR-008`). `-02` and `-13` are **both charted and both correct numbers**, so both banks passed — and go on passing. **A registry gate proves a tag EXISTS; it cannot prove the tag is the RIGHT ONE for the slot the item sits in.** The suite was CLEAN over this for four waves. **(d) THIS IS `QB-CR-008`'s LINEAGE, ARRIVING FROM THE OPPOSITE DIRECTION.** `QB-CR-008` was a tag **inferred from a same-numbered field in a different scheme** (`S11` read as `TOP-…-11`) and it produced `-13`'s minting. This row is the **under**-correction that the same minting left behind: `-13` was minted, charted, and gated — **and the eight items it was minted for were never moved onto it.** **A number minted for a purpose is not in use until something carries it**, and nothing checked that. **(e) EXECUTED HERE, metadata only, and proved rather than asserted.** `authoring/correct_U13_S11_tags.py` rewrites `topic_tag` on items whose `slot_index` entry is exactly `S11` and whose tag is exactly `-02`, **asserts per item that `topic_tag` is the only field that moved**, refuses on any count other than 8, and is idempotent. **No stem, key, `bloom_level`, `difficulty`, `marks`, `paper_role` or `ref19_topic_id` is touched.** `header.topics` gains `-13`; `-02` stays declared and is still supplied by 16 `S03` items. **(f) `ref19_topic_id` STAYS `BAN-SENTENCE` AND THAT IS NOT AN OVERSIGHT.** REF-19 carries **no punctuation slug at all** — `PENDING-P-008`, FLAGGED — so there is nothing to move it to, and minting one here would be `QB-CR-008`'s error committed in the other register. **Two registers, two identities** (CD-108): the `TOP-` number moves, the REF-19 slug does not. **(g) THE WAVE SCRIPTS ARE NOT EDITED.** `author_U13_wave3.py` keeps `SENT = "TOP-BAN-C5-02"`. Editing it would make the record say wave 3 got it right, destroying exactly the evidence this ledger exists to preserve — `QB-CR-009`'s standing reason, applied to its own lane. **The bank is reproducible as waves 1–4 THEN this correction script**, and the two-step is the record. **(h) SIBLINGS CHECKED SAME-SESSION (AGENTS §6).** পাঠ ১৪: six `S11` items, already `-13`, correct. পাঠ ২১: `Q52` is the only বিরামচিহ্ন item, already `-13` — **it is the item `QB-CR-008` was written about and it was corrected then**, which is why পাঠ ১৩, authored later, is the outlier rather than the origin. **No third instance; this is 2 of 3 against `QB-CR-008`'s shape and not promoted.** | Agent, authoring পাঠ ১৪ (2026-08-16); reported at `fe60e25` and in that session's `SESSION_LOG.md` block; ruled for execution by the Principal 2026-08-16 | **CLOSED 2026-08-16** — eight tags corrected, all four export artifacts regenerated to a new digest, signature row amended to the correcting commit with the sign-off standing (questions unchanged). **Standing lesson: a registry gate proves a tag exists, never that it is the right one — a number minted for a slot must be checked INTO USE, and nothing does that today.** |
| QB-CR-015 | 2026-08-16 | **`QB-CR-012`'s PATTERN TAKES ITS FIFTH INSTANCE — `CD-141(c)`'s `N/A` COUNT REPORTED FROM THE DECISION ROW INSTEAD OF FROM THE GATE OUTPUT THAT WAS ON SCREEN AT THE TIME (Principal direction 2026-08-16).** *(Next free verified at source before writing: this ledger defines through `QB-CR-014`; no `QB-CR-015` token existed anywhere in the repo outside this row. **`QB-CR-012`'s own row is UNEDITED** — append-only, AGENTS.md §4; its status cell still reads *promoted at 4 instances* and that is correct history. **This row is the forward-only append that carries the fifth, which is the same form `QB-CR-012` used to carry `QB-CR-011`'s** — the instance is added to the pattern by a new row citing it, never by editing the row it belongs to.)* **(a) WHAT HAPPENED.** The 2026-08-16 session report asserted *"8 `N/A`, all of them the qp6-shape gates CD-141(c) names"*. **A live run prints TEN.** The figure was taken from `CD-141(c)`'s own illustrative sentence rather than counted from the suite output the session had just produced. **(b) WHY IT BELONGS TO THIS PATTERN AND NOT TO A NEW ONE.** `QB-CR-012` names the pattern as **reading a surface instead of the thing underneath**. Instance 3 read a marker string as a task mode; instance 4 read a handoff summary as the row it summarised. **Here the surface is a decision row's own illustrative figure and the thing underneath is the gate output** — same shape, new surface, and the surface is one the previous four instances would not have warned against, because a CD row is normally exactly the authority a session is told to read at source. **(c) ALREADY RULED, AND THIS ROW DOES NOT RE-RULE IT.** `CD-145` (2026-08-16) corrects the count to **TEN**, leaves `CD-141`'s text unedited, and confirms the operative test — *no gate that SHOULD judge returned `N/A`* — is **unchanged and independent of the count**, so **no teacher-lane verdict is re-opened**. `CD-145(e)` is where the instance is recorded as belonging to this pattern; **this row is what puts it in the ledger the pattern lives in**, so a reader counting instances here finds five rather than four. **(d) THE RESIDUE GAINS ONE WORD, quoted from `CD-145(e)` rather than restated.** `QB-CR-012(d)`'s behavioural residue — *do not cite a §-number, a CD row or a policy scope you have not read AT SOURCE in this conversation* — is extended: **a COUNT is not read at source, it is COUNTED.** The standing form is `CD-145(f)`: *an illustrative count inside a ruling is verified by running the thing it counts, in the session that writes it, or it is not written.* **(e) NO GATE IS PROPOSED, and the reason is `CD-145`'s own:** a gate cannot know which figure in a sentence was meant to be a measurement. **The pattern's standing position is unchanged — it is closed one specific inference at a time, in the row that builds it.** | Principal direction 2026-08-16; `QB-CR-012` (unedited); `QB-CR-011`; `CD-145`, esp. (e) and (f); `CD-141(c)` (text unedited, count corrected by `CD-145`); AGENTS.md §4, §6 | **PATTERN — fifth instance, appended forward-only. No gate proposed; `CD-145` carries the executable form.** |
| QB-CR-016 | 2026-08-16 | **`git mv` SILENTLY PRE-STAGED 65 FILES INTO A COMMIT DECLARED RULING-ONLY — the class boundary CD-141(b) draws twice was crossed by the INDEX, not by a decision (agent error, caught same-session before any push; row authorized by the Principal 2026-08-16).** *(Next free verified at source before writing: this ledger defines through `QB-CR-015`; no `QB-CR-016` token existed anywhere in the repo outside this row.)* **(a) WHAT HAPPENED.** The CD-148 archive move was performed with `git mv`, which **stages as it moves**. Some minutes later the CD-147/CD-148 decision rows were committed with `git add canon/DECISIONS.md && git commit`. **`git commit` without `-a` commits the whole index, not the paths just added** — so the ruling commit carried **66 files: `canon/DECISIONS.md` plus all 65 U21 archive paths**. The promotion-class commit that should have carried them then found nothing to commit and printed *nothing to commit, working tree clean*, **which is what surfaced it** — the error announced itself only through the SECOND commit's silence, not the first's contents. **(b) WHY IT MATTERS HERE MORE THAN IN AN ORDINARY REPO.** `CD-141(b)` draws the push boundary **twice, by class AND by path, precisely because either test alone lets one error through**. A commit mixing ruling-class and promotion-class defeats both tests at once: **CD-079's ruling-only carve-out is voided** (the diff is no longer text the Principal ruled), and the per-commit range check `CD-141(j)` requires would have reported a class the commit does not actually hold. **The stack would have been approved on a description that was wrong.** **(c) NOTHING WAS PUSHED, and the repair is recorded rather than hidden.** The stack was rebuilt with `git reset --mixed` to the last legitimate commit and re-committed one class at a time, on **`QB-CR-013`'s precedent** — that row's own remedy was *the premature commit was dropped by `git reset` (unpushed; none of its text reused)*. `CD-141(f)` lists `git reset` as never pre-approved **in the teacher lane**; this was local repair of the agent's own staging error before any approval was sought, and it is filed here rather than left in chat so the reset is on the record. **(d) THE DEFENSIVE RULE, RULED BY THE PRINCIPAL AND EXECUTABLE BY A HUMAN, NOT A GATE:** **`git status` is pasted immediately before EVERY commit in a multi-class session.** Not once at the start, not after — **immediately before each one**, because the index is what the commit will take and the index is the thing that drifted. **A gate cannot catch this**: at commit time the staged tree is internally consistent and every file in it is a file the session legitimately changed. Only the *intended class* is violated, and intent is not in the repo. **(e) THE TRANSFERABLE SHAPE, which is `QB-CR-012`'s pattern in a new place.** The commit message is a SURFACE; the index is the thing underneath. **An agent that writes `-m "Ruling: …"` has described what it meant to commit, not what it committed** — and every downstream check in this repo, including the range check that exists to catch exactly this, reads the message. **Not counted as a sixth instance**: `QB-CR-012`'s pattern is about reading a source wrongly, and this is about not reading one at all. Recorded as adjacent, for the Principal to fold in or not. | Agent error 2026-08-16, caught same-session by the second commit reporting an empty tree; repaired before any push; row authorized by the Principal 2026-08-16 | **CLOSED 2026-08-16** — stack rebuilt to one class per commit and pushed at `4635098` after the per-commit range check. **Standing rule: `git status` immediately before every commit in a multi-class session.** No gate proposed; the violated thing is intent, which is not in the repo. |
| QB-CR-017 | 2026-08-17 | **THIRTEEN `S11` ITEMS WERE AUTHORED ACROSS FOUR SESSIONS AGAINST A SHAPE THE SLOT'S OWN RULE BARS — EACH REQUIRES A SINGLE TERMINAL MARK — AND NO GATE SAW ONE OF THEM. THE GAP IS THE FINDING, NOT THE THIRTEEN EDITS.** *(Next free verified at source immediately before writing: this ledger defines through `QB-CR-016` on a freshly fetched `origin/main` (`dd40a93`) and no `QB-CR-017` token existed anywhere in the repo or in the held commits.)* **WHAT WAS AUTHORED.** `U13` Q58 · Q61 · Q89 · Q90 · Q91 · `U14` Q65 · Q68 · `U15` Q76 · Q77 · Q78 · Q79 · Q82 · `U16` Q81. Every one asks *প্রয়োজনীয় বিরামচিহ্ন বসিয়ে লেখো* and every one is satisfied by placing ONE mark — a দাঁড়ি, a কমা, a প্রশ্নচিহ্ন or a বিস্ময়চিহ্ন at the end. The vendored review's own S11 clause has asked *does each item require more than a single terminal mark?* since it was written, and it caught them one bank at a time, in four separate runs, months of session-time after the first was authored. *(The brief that ruled the fix says TWELVE items and then lists THIRTEEN qids; the list is what this row counts, and the discrepancy is recorded rather than silently resolved.)* **WHY NO GATE SAW IT, AND THIS IS THE PART WORTH A ROW.** `PLAN` and `COVERAGE` between them check that S11 exists, that it supplies its full `items_per_paper`, that each item declares an admitted task, that its marks match the register and that its Bloom tag clears a floor. **All five were true of all thirteen.** The item's WORK — how many marks the student must actually place — is a quantity no gate in the suite computes, because every gate reads the item's METADATA and this defect lives in its PROSE. **A bank can be fully conformant and still ask for a fifth of the work the slot is worth**, and the arithmetic makes it worse: five one-mark items, each satisfiable by one দাঁড়ি, is a 5-mark question paying 5 marks for five keystrokes. **THE PATTERN IT BELONGS TO — `QB-CR-012`'s family, and its SIXTH instance (`QB-CR-015` was the fifth): A CHECK THAT READS THE DECLARATION INSTEAD OF THE THING.** Same shape as CD-141(c)'s N/A count read off a decision row, and as `TOOLS-CR-008`'s report-by-item against a defect-by-claim. Here the declaration is the metadata and the thing is the sentence a child is asked to punctuate. **A review clause caught what the whole gate suite could not, and the gate suite is what runs unattended.** **PROPOSED PROMOTION, and it is deliberately narrow (`AGENTS §6` — 3+ occurrences → propose an executing gate).** A gate CAN count this without judging prose: for an S11 item, count the positions where a mark must be inserted by diffing the stimulus against the accepted answer, and FAIL a count of one. **It is mechanical, it needs no new register field, and it would have fired on all thirteen the day each was authored.** Not built here: the same measurement that produced this row also produced CD-165(g), where the marks a class may use are still open at four items, and a gate counting marks should not land before the set of admitted marks is closed. **FIX STATUS — HELD, and the reason is a ruling and not a difficulty.** `CD-165(g)` leaves উদ্ধরণ চিহ্ন neither admitted nor barred at C5, and eight of the thirteen would be re-stemmed against a taught set that may still change. **Measured while checking feasibility, and reported so the next session is mechanical rather than exploratory:** পাঠ ১৩'s poem prints only কমা · দাঁড়ি · বিস্ময়চিহ্ন, so its five are re-stemmable from printed lines; **পাঠ ১৫'s poem prints ড্যাশ and সেমিকোলন in four of its lines**, so its five have very few admitted-only multi-mark spans and the few there are OVERLAP each other, which would buy a `REPETITION` exposure to close an S11 one; and `U13` Q91, `U14` Q68 and `U16` Q81 are AUTHORED question and exclamation sentences rather than printed ones, each carrying exactly one natural mark, so a multi-mark form for them cannot come from the section at all. | Principal ruling 2026-08-17 (fix ruled, held on CD-165(g)); `tools/bank_factual_review_prompt.md` check 6 · S11; four review runs (`BAN_U13_REVIEW_2026-08-17_RUN9`/`RUN10`, `BAN_U14_*_RUN3`, `BAN_U15_*_RUN3`, `BAN_U16_*_RERUN`); CD-165 + `SLOT_REGISTER` (BAN, C5, BAN-S11); `QB-CR-012` · `QB-CR-015` · `TOOLS-CR-008` (the pattern); `MarkLogic_BAN_Spine.md` · `BAN-S11` | **OPEN** — thirteen items unfixed, pending the উদ্ধরণ চিহ্ন ruling at CD-165(g); the PLAN-vs-review gap itself is recorded and its gate proposed, not built |
| **QB-CR-018 · PATTERN GENERALISED** | 2026-08-18 | **`QB-CR-012`'s PATTERN IS RESTATED AT ITS REAL WIDTH AND ITS COUNT IS RECONCILED — the family was being counted THREE TIMES IN PARALLEL, by three ledgers that did not know about each other, which is `CD-160`'s list-versus-class finding landing on the pattern-tracking mechanism itself (Principal direction 2026-08-18; AGENTS.md §6; `CD-088`'s promotion form).** *(Next free verified at source immediately before writing, per CD-154: this ledger defines through `QB-CR-017` on a freshly fetched `origin/main` (`23e3d1d`) and no `QB-CR-018` token existed anywhere in the repo. **`QB-CR-012`'s row is UNEDITED** — append-only, AGENTS.md §4; its status cell still reads *promoted at 4 instances* and that is correct history. This row is the forward-only append that carries the widening, the same form `QB-CR-015` and `QB-CR-017` used.)* **(a) THE PATTERN, STATED AT ITS REAL WIDTH.** **AN INSTRUMENT ESTABLISHES ONLY WHAT ITS OWN SURFACE TOUCHES. A check, a report, a seed, a verdict or a ruling's prediction is evidence about the surface it ACTUALLY READS — never about the claim it was AIMED AT, unless the two surfaces are shown to coincide.** **`QB-CR-012` minted this as *reading a surface instead of the thing underneath*, and that form is correct but narrower than the evidence now supports: it describes a READER — an agent taking a marker string for a task mode, a handoff summary for the row it summarises. The six instances since are not all readers.** One is a CHECK drawn narrower than the defect (`QB-CR-017`), one a REVIEW RUN whose scope excluded the field the defect sat in (`CD-168`), one a VERDICT quoted without the artifact it was obtained against (`CD-170(e)`), one a RULING PREDICTING a gate's behaviour without running it (`CD-171(e)`), and one a SEED asserting where a value is CREATED while the claim was about where it is READ (`CD-172(e)`). **The common term is not *a reader who looked at the wrong thing*. It is that EVERY INSTRUMENT HAS A SURFACE, AND ITS AUTHORITY STOPS THERE.** **(b) THE COUNT, RECONCILED — AND THE RECONCILIATION IS THE FINDING WORTH MORE THAN THE RESTATEMENT.** **Three independent running counts of ONE family were live in this repo simultaneously**, each internally consistent and each unaware of the others: **(i)** `QB-CR-012`'s, promoted at 4 and carried to 5 by `QB-CR-015` and to 6 by `QB-CR-017`; **(ii)** `CD-168`'s, which names the shape afresh and is called *the fourth instance of CD-168's pattern* by `CD-169(e)`; **(iii)** `CD-170(e)`'s *fourth instance of one shape*, continued as *fourth* at `CD-171(e)` and *fifth* at `CD-172(e)`. **So `CD-170(e)` and `CD-169(e)` both say FOURTH, of different fourths, of the same family.** **A pattern counted in three places is a pattern nobody can tell the size of**, and size is the whole of AGENTS §6's promotion trigger. **RULED: `QB-CR-012`'s IS THE COUNT OF RECORD**, being the earliest and the only one filed as a promotion. The roster, dated, is: **1** `CR-007` (2026-08-11) · **2** `QB-CR-011` (2026-08-15) · **3** CD-138(1) as first drafted (2026-08-15) · **4** the CD-136 scope misread (2026-08-15) · **5** `QB-CR-015` (2026-08-16) · **6** `QB-CR-017` (2026-08-17) · **7** `CD-166(c)` (2026-08-18) · **8** `CD-168` (2026-08-18) · **9** `CD-170(e)` (2026-08-18) · **10** `CD-171(e)` (2026-08-18) · **11** `CD-172(e)` (2026-08-18). **`TOOLS-CR-008` (2026-08-17) IS AN INSTANCE AND IS DELIBERATELY LEFT UNNUMBERED HERE** — it is same-dated with `QB-CR-017`, it lives in a different ledger under a different prefix, and inserting it at a guessed ordinal would assert an order nothing supports. **The membership is ruled; the ordinal is not, and the two are recorded separately rather than folded** (`CD-090(b)`'s principle, `CD-133(c)`'s discipline). **No count is edited into any row that carries one**; the wrong-looking ordinals at `CD-169(e)`, `CD-170(e)`, `CD-171(e)` and `CD-172(e)` stay as filed and are correct history of what each session could see. **(c) THE STANDING REMEDY, STATED AS A RULE AND NOT A LIST.** **BEFORE ANY INSTRUMENT IS RELIED ON, NAME ITS SURFACE AND NAME THE CLAIM'S SURFACE, AND STATE WHY THEY COINCIDE.** **Where they cannot be shown to coincide, the instrument is REPORTED AS PARTIAL AND THE RESIDUE IS NAMED.** **A rule rather than a checklist, deliberately.** `QB-CR-012(c)` closed its own version one inference at a time — *a pattern that cannot be gated is closed by writing the specific inference out of the specific gate* — and eleven instances say that a list of specific inferences is always one short of the next one. **The rule costs one sentence per instrument and it is falsifiable: either the coincidence is stated or it is not, and a reader can check.** **`TOOLS-CR-008` IS SUBSUMED AS ONE CASE AND IS NOT RETIRED — it is CITED AS THE FAMILY'S FIRST FULLY-WORKED INSTANCE**, the only one carried all the way from defect to stated remedy of practice: *a report is organised by ITEM because that is what a reviewer reads; a factual defect is organised by CLAIM* — its own §(2), *grep the claim, not the id*, is exactly this rule with the two surfaces named. **Its row stands unedited and remains the worked example a session should read first.** **(d) THE CLAUSE SPECIFIC TO THE NEWEST VARIANT — the one no earlier instance covers.** **A SELFTEST THAT DOES NOT ASSERT THE LIVE SWEEP'S OWN OUTPUT PROVES THE APPENDER, NOT THE GATE.** **ANY GATE WHOSE REPORTS MUST REACH A HUMAN ASSERTS THE PRINTED SWEEP, SEPARATELY FROM THE APPENDER**, as `CD-172(e)` now does. **The instance, measured:** `CD-172`'s named-exclusion reports were built correctly by `g_coverage`, passed four seeded cases naming both excluded ids, and **reached nobody** — the bank sweep's printer forwarded only `BLOOM-BAND` lines, so the run that decides a push showed a silent skip while the selftest showed green. **The surface the seed touched was the `rep` list; the claim was about the operator's screen.** Found by grepping the sweep's output for the row's own name, not by trusting the selftest. **This is the first instance in the family where the instrument and the claim were separated by a FILTER rather than by a misreading** — nobody read anything wrong; a correct value was produced and then discarded downstream, which no amount of care at the point of production would have caught. **(e) NO GATE IS PROPOSED, and the reason is `QB-CR-012(c)`'s, unchanged and now better evidenced.** A gate cannot know what claim an instrument was aimed at — that is a fact about intent, and `AGENTS §5.1`'s own constraint applies: a gate must not make naming the defect unwriteable, and a gate policing surface-coincidence would have to read prose about surfaces. **What IS executable is (d), and it is narrow, mechanical and lands in each gate as it is built.** The ledger's own promotion trigger — *3+ occurrences → propose promotion to an executing gate* — is satisfied and answered: **the proposal is made and declined on the record, which is not the same as not proposing one.** **(f) SINGLE COUNT OF RECORD — appended 2026-08-18 by Principal direction, after the push that carried (a)–(e).** **A PROMOTED PATTERN HAS EXACTLY ONE ROSTER, IN THE LEDGER WHERE IT WAS PROMOTED.** **Any session citing an ORDINAL for a promoted family READS THAT ROSTER AT SOURCE FIRST.** An ordinal asserted from what the session happens to have in front of it is how three counts arose — and each of the three was **internally consistent**, which is why none of them looked wrong from inside. **WHERE A SESSION CANNOT LOCATE THE ROSTER, IT RECORDS THE INSTANCE WITHOUT AN ORDINAL AND SAYS SO.** **An unnumbered instance is recoverable; a wrong ordinal propagates** — it is cited, carried into a handoff, and read by the next session as the count of record, which is exactly the route `CD-169(e)` → `CD-170(e)` → `CD-171(e)` → `CD-172(e)` took in a single day. **This binds `QB-CR-012`'s roster specifically — carried at (b) of this row — and every future promotion.** **AND THE CLAUSE IS ITSELF THE FAMILY'S OWN SHAPE, LANDING ON THE MECHANISM THAT TRACKS THE FAMILY. Recorded on the row rather than left for a reader to notice, because it is the strongest evidence (a) is stated at the right width.** Each of the three counts was **a correct reading of its own surface**: `CD-168` correctly counted the instances `CD-168` names; `CD-170(e)` correctly counted the shape `CD-170(e)` had just met; `QB-CR-012`'s roster correctly counted what the question-banks ledger holds. **Nobody misread anything. Every instrument was sound about the surface it touched, and none of them touched the family.** The claim — *how many instances does this pattern have* — has a surface no single row carries, and (c)'s rule is what would have caught it: **name the instrument's surface (this row's own citations) and the claim's surface (the promoted roster), and state why they coincide.** They did not coincide, nothing said so, and the residue went unnamed **eleven times before it was counted once.** **THE SELF-APPLICATION IS NOT A FLOURISH.** A pattern about instruments overreaching their surfaces was tracked by rows that overreached theirs, for three days, in the repo that minted it — which is `QB-CR-012(b)`'s own promotion argument arriving again: *a pattern is not promoted because it is rare; it is promoted because knowing it does not prevent it.* **Knowing it did not prevent it here either. What (f) adds is not knowledge but A PLACE TO LOOK**, and a rule that fails LOUDLY — an unnumbered instance is visible as unnumbered — rather than quietly, which is the only difference between the two failure modes this clause chooses between. | Principal direction 2026-08-18; **`QB-CR-012`** (the pattern, promoted at 4, row UNEDITED) · **`QB-CR-011`** (candidate) · `CR-007` · **`QB-CR-015`** (5th) · **`QB-CR-017`** (6th); **`TOOLS-CR-008`** — SUBSUMED AS ONE CASE, **NOT RETIRED**, cited as the first fully-worked instance; `CD-166(c)` · `CD-168` · `CD-169(e)` · `CD-170(e)` · `CD-171(e)` · `CD-172(e)`; **`CD-160`** (list versus class — why three counts drifted); `CD-090(b)` + `CD-133(c)` (why membership and ordinal are recorded separately); `CD-088` (promotion form); `CD-138(b)`/`(e)` (the executable residue of instance 3, unchanged); AGENTS.md §4, §5.1, §6 | **PATTERN — RESTATED AND RE-COUNTED AT 11 NUMBERED INSTANCES plus `TOOLS-CR-008` unnumbered. `QB-CR-012`'s count is THE COUNT OF RECORD; no other row's ordinal is edited. NO GATE CHANGE — the standing remedy at (c) is behavioural, the executable clause at (d) binds each gate as it is built, and (f) SINGLE COUNT OF RECORD binds every session that cites an ordinal for a promoted family. (f) was appended AFTER (a)–(e) were pushed at `3ba8901`; the append is forward-only and (a)–(e) are UNEDITED.** |
| QB-CR-019 | 2026-08-19 | **A PRINTED অনুশীলনী QUESTION WAS AUTHORED TWICE — ONCE AS ITS OWN S07 ITEM AND ONCE AS AN S05 MCQ STEM — AND THE TWO WERE BYTE-IDENTICAL ABOVE `Remember`.** পাঠ ২০'s extraction prints five numbered প্রশ্নোত্তর at অনুশীলনী ৪; `ঘ. শিক্ষক কী ভেবে মনে সাহস পেলেন?` became `QP-BAN-C5-U20-Q32` (S05, `Understand`) and `QP-BAN-C5-U20-Q44` (S07, `Understand`) with the same stem. §5 permits verbatim reuse ONLY at `Remember`, so `REPETITION` FAILed and the bank did not ship in that state. **THE CONDITION IS STRUCTURAL, NOT A SLIP.** A chapter whose extraction prints a numbered question list gives the author the same sentences twice over: they are the natural S07 items because the source already framed them as short answers, and they are the natural MCQ material because their answers are already settled. An author working slot by slot reaches the list twice and has no reason, inside either slot, to notice. **AND THE PLAN TABLE DID NOT CATCH IT** — the slot-by-slot plan named S05 and S07 separately, each correctly, and the collision existed only BETWEEN them, which is the axis a per-slot plan does not look along. | **RESOLUTION, APPLIED IN THE SAME SESSION AND BEFORE THE BANK WAS OFFERED.** The printed exercise question stays where the source put it — S07, short answer, 2 marks. The S05 item was re-stemmed to `পাঠ ২০ অনুযায়ী শিক্ষক কোন কথা মনে করে নিজের ভয় কাটিয়ে উঠলেন?`, which asks the same content through a different sentence. Suite re-run: CLEAN, `RUNALL_SENTINEL=CLEAN`, 80 items. **THE STANDING NOTE, and it is an authoring rule rather than a gate proposal:** when an extraction prints a numbered প্রশ্নোত্তর list, those sentences belong to S07 and are not available verbatim to any other slot above `Remember`. **NO GATE IS PROPOSED** — `REPETITION` already executes this and executed it here on first run. What this row records is that the gate, not the plan, is what saw it, so a future author who trusts a per-slot plan to be sufficient has this instance to read. **First defect found by the chat BUILD lane on its own output (CD-177), and it was found by a gate, not by the chat.** | FILED 2026-08-19 · resolution APPLIED, standing note PROPOSED |
| QB-CR-020 | 2026-08-19 | **REVIEW FOUND TEN DEFECTS IN A BANK EVERY GATE HAD PASSED, AND THREE OF THEM WERE FACTUALLY FALSE STATEMENTS TO A STUDENT.** `C5_BAN_U20_QuestionBank_v1.json`, published at `dd7077e` with `RUNALL_SENTINEL=CLEAN`, was read item by item by an independent REVIEW chat. Verified at source before any correction: **(1) `Q73` offered `চিহ্ম` as a word — it is not one, and চিহ্ন is হ্+ন with no হ্ম in it at all.** **(2) `Q36`/`Q37` told the student that উন্নত and মহান appear in পাঠ ২০-এর শব্দতালিকা; the glossary has 38 headwords and neither is among them.** **(3) The bank header claimed ৩৯টি শব্দার্থ — the count is 38, and the error was the AUTHOR'S OWN, missed by the review too and caught only on re-counting at source.** Also: `Q20–Q27`, eight verbatim gap-fills tagged `Understand` when the task is recall; `Q76–Q80`, the glossary read backwards and tagged `Understand`, with `Q79`/`Q80` exact inverses of `Q06`/`Q07`; all eight `S05` MCQs carrying distractors whose only fault was *not in the poem*, so every one was answerable by elimination without reading the poem; `Q65`/`Q68` requiring a comma the poem does not print; `Q40` keying অন্যে, unattested; `Q01` at `medium` and `Q52` at `Create`, both overstated. **NOT ONE OF THESE IS DETECTABLE BY ANY GATE IN THE SUITE, AND THE SUITE WAS GREEN THROUGHOUT.** `BLOOM-BAND` checks that a tag is one of six, never that it is the RIGHT one. `SOURCE-TRACE` checks that an anchor resolves, never that the stem's claim about the source is true. `KEY-RUBRIC` checks that a key exists, never that its content is a real Bangla word. | **CORRECTED IN THE BANK, at `dd7077e+1`, before any teacher or Hub import used it — this is a CORRECTION to an unread artifact, not a rewrite of taught material.** `Q73` keyed to ব্রাহ্ম · ব্রাহ্মণ; `S06` rebuilt on five words verified present in the 38-word list (সাফ · তফাতে · প্রাতে · উচ্চরবে · স্বয়ং); header corrected to ৩৮; `S04` and `S13` retagged `Remember`; `S13`'s two inverted clues replaced with শির and নিরালায়, which no `S02` item uses; **all eight MCQs rebuilt with distractors drawn from the poem's own content — the এলিমিনেশন route is closed and each `why_wrong` now teaches the confusion it corrects**; `S11` rebuilt so `accepted[0]` is ALWAYS the form the poem prints, with `Q68` carrying the comma-free printed form first and the comma form as an accepted variant; `Q65` re-stemmed onto the ভাবিলেন line, whose two commas ARE printed; `Q40` keyed পরের দ্বারা; `Q01` → `hard`; `Q52` → `Evaluate`. Suite re-run CLEAN, export regenerated, batch digest `3484d4f87fdc`. **THE LESSON IS THE ONE PLAN §9 ALREADY STATED AND THIS ROW NOW ATTESTS: `REVIEW` IS NOT MADE REDUNDANT BY A GREEN SUITE.** The BUILD chat authored these, ran 24 gates against them, and reported CLEAN in good faith — because every mechanical check passed. **A green hook proves the artifact is well-formed; it proves nothing about whether the artifact is true.** First REVIEW ever run in the chat-native lane (CD-177); it found ten things on its first outing. `D-022`'s no-self-review rule is now attested rather than argued. | FILED 2026-08-19 · bank CORRECTED, `Q55`/`Q61` ruling APPLIED (see CD row owed) |
