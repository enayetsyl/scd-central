# DECISIONS.md — Support-Book Programme Decision Log

<!-- CD-124: the prefix is what rows in this file ALREADY mint; the lane identifies this ledger. Declared now, renumbered per lane at close — no row is renumbered by this line. -->
<!-- ledger-prefix: D -->
<!-- ledger-lane: support-books-decisions -->
*Append-only. One row per decision. Never edit or delete a row — reversals get a NEW row citing the old ID. The `D-` series is the only decision series in this project; it is self-contained.*

**Founding note.** The programme was re-founded in July 2026 on a redesigned production flow. This log opens fresh with the `D-` series. Every content, safety, and posture ruling the programme depends on is re-asserted below as a founding decision — the rules stand on their own merits, not on any retired prior series. The earlier process apparatus (per-book ledger chats, per-পাঠ brief documents, a separate MERGE chat, front-loaded freeze ceremony) is **not** carried forward; it is replaced by D-001.

**Row format:** ID · Date · Decision (one sentence) · Rationale (short) · Scope/affects · Status (ACTIVE / SUPERSEDED-BY-xx) · Approved by

| ID | Date | Decision | Rationale | Affects | Status | Approved |
|---|---|---|---|---|---|---|
| **D-001** | 2026-07 | Production runs a **nine-step per-chapter loop**: (1) compliance map → (2) Principal ruling on action flag + codes → (3) content → (4) image prompt (prompt only, no stripe language) → (5) image approval against the manifest → (6) image ref recorded (stripe/upscale/crop done by software outside the chat) → (7) full lesson object emitted → (8) validator **script executed** → (9) merge on pass. One chapter per chat or several while context stays clean; **merge after every chapter**. | Collapses the old ledger/brief/CONTENT/MERGE ceremony into one continuous chat while keeping every gate. | Whole workflow | ACTIVE | Principal |
| **D-002** | 2026-07 | **Per-book setup:** pick a book, upload its TG Reconciliation + skeleton, chat prunes to book-relevant content and saves cleaned copies to Production; generate any *absent* reference images (the class cast if this is the class's first book, plus book-local figures). | Lean, chat-driven start; no standing cross-project reference needed. | Setup | ACTIVE | Principal |
| **D-003** | 2026-07 | **The book JSON is the single source of truth; the ledger is discarded**, not kept as a file. Every ledger field lives in the lesson object; the compliance map is derived from the merged JSON. Reviewer sign-off is recorded in a `reviewer_signoff` field (by · date · checklist_passed) in the lesson object. | The JSON already carries every ledger field; a separate ledger is redundant once sign-off moves into it. | Schema, workflow, review | ACTIVE | Principal |
| **D-004** | 2026-07 | **One validator script**, not one per subject. All checks are subject-agnostic except the letter audit, which runs for Class 1–2 বাংলা by loading that book's `letter_inventory` data file and is skipped elsewhere. The validator runs as an **executed script (never reasoned judgment)** and must pass the **seeded-error test** before any chapter merges through it. | Determinism is the whole point of the audit; a model reasoning about letters is not a net. | Validator, C1–2 books | ACTIVE | Principal |
| **D-005** | 2026-07 | **Two-mode model:** Mode-R (replacement) for classes 1–4 and 6–7; Mode-C (curation-only) for classes 5, 8, 9–10. Mode-C books never contain `replace`; work is image curation + S1 text touches only. | Exam classes (বৃত্তি, board) need NCTB content fidelity. | All books | ACTIVE | Principal |
| **D-006** | 2026-07 | **সহায়িকা posture:** support books are internal and free, presented and genuinely used as সহায়িকা alongside the NCTB book, which keeps its scheduled functions; our books carry NCTB page cross-references. | Truthful, verifiable posture; lowers legal + partner-school risk. | Posture, book design | ACTIVE | Principal |
| **D-007** | 2026-07 | **Product separation (absolute):** support books never enter the storybook sales catalog, storefront, or any commercial channel; separate folder roots; no exceptions. | Copyright posture requires separation. | Tooling, folders | ACTIVE | Principal |
| **D-008** | 2026-07 | **Per-পাঠ action flags** `retain` / `retain-curated` / `replace`: not all content is replaced; the compliance map decides per পাঠ; minimum sufficient action; `replace` is last resort. Validator behaviour is keyed to the flag. | Effort proportional to conflict. | Content, validator | ACTIVE | Principal |
| **D-009** | 2026-07 | **Taught-letter constraint** (Class 1–2 বাংলা decodable text only): replacement decodable text for পাঠ N uses only letters + কারচিহ্ন taught up to N; B-1 যুক্তবর্ণ whitelist (only a পাঠ's own conjuncts, or none); oral (শুনি-ও-বলি) text exempt. Enforced mechanically by the validator's letter audit. | Human review cannot reliably catch letter violations. | C1–2 বাংলা, validator | ACTIVE | Principal |
| **D-010** | 2026-07 | **Image doctrine:** objects-first substitution; programmatic white stripe (applied by software outside the chat, **never** in prompts) where figures must remain; schematic redraw for pedagogical diagrams; photos replaced; dress/modesty rules (adult male full beard + hem above ankle, female full coverage face+hands only). **Prophets, angels, and Sahaba are never depicted (absolute).** | Reuses proven compliance step; protects শিখনফল in diagrams; creed line on depiction. | All images | ACTIVE | Principal |
| **D-011** | 2026-07 | **Islamic-content rule:** narratives stay within famous, well-known narrations; drafts carry a brief `source_note`; **narration details are never invented — uncertainty is flagged, not filled.** Reviewer sign-off is the single content sign-off; reviewer consults an আলিম on source/creed questions. No separate verification gate. No Arabic script in JSON strings. | Fabricated narration in print is the worst failure mode; leaner review than a standing gate. | All narrative content | ACTIVE | Principal |
| **D-012** | 2026-07 | **Anthem/flag content omitted** from support books entirely (anthem front-matter, flag-focused content; mixed পাঠ keep their non-flag elements). Students meet this in the NCTB book. The C-18 factual-history carve-out (মুক্তিযুদ্ধ, civic facts) is retained. | Clean line; avoids per-পাঠ reframing debates. | REF-1, all books | ACTIVE | Principal |
| **D-013** | 2026-07 | **Recurring cast is per class:** each class has its own four recurring children drawn from that class's Name Bank pool, generated once (at the class's first book) and reused across that class's subjects. Cast scenes are same-gender (no mixed-gender pairings); one cast child keeps a mobility aid. Reference images are canon. **C1 cast:** উমর, আনাস, খাদিজা, ফাতিমা (ফাতিমা keeps the mobility aid); C2–C10 casts named as each class is reached. | Consistency within a child's year; matches the per-class Name Bank structure. | REF-2 §2, all books, images | ACTIVE | Principal |
| **D-014** | 2026-07 | **Curation only via REF-1 codes; names only from REF-2.** Content chats never improvise curation policy or invent names; a case fitting no code is S4 (escalate to Principal). REF-1 (curation taxonomy) and REF-2 (Name Bank + per-class cast) remain the locked data authorities. | Nothing flagged or named without an authority. | REF-1, REF-2, content | ACTIVE | Principal |
| **D-015** | 2026-07 | **Governance is five files** — README, DECISIONS, SCHEMA, REF-1, REF-2 — with the Master Guide's rules absorbed into README + these founding decisions. Production holds working artifacts only. `support-book.json` v1 schema (one-file model, prompts in slots, per-পাঠ lessons, provenance blocks, wholesale-by-lesson merge, letter-inventory companion, `reviewer_signoff` field) is the JSON contract. | Lean structure; the schema already anticipated this flow. | Structure, schema | ACTIVE | Principal |
| **D-016** | 2026-07 | **Two print editions, both first-class and always rendered** from the same validated JSON: **print-colour** (colour, true white) and **bw-photocopy** (greyscale/line, true white, high-contrast, copier-verified). Because the B/W edition must always work, **image discipline stays live** — high-contrast/line-art-capable art and B/W-safe redesigns of colour-coded pedagogy are retained regardless of colour printing. Each lesson carries `bw_treatment` ∈ {native_safe, redesigned, print_only_omit}; the assembly validator red-fails a B/W edition with unresolved colour-pedagogy pages. | Colour master where it helps + cheap B/W for bulk photocopying, without betting sustainability on colour-print budget; downstream photocopying happens regardless. | Schema, assembly, images | ACTIVE | Principal |
| **D-017** | 2026-07 | **The Bengali-safe renderer is a shared neutral build tool, not shared product infrastructure** — reused across the storybook and support-book projects like a compiler or font. It holds no content, has no product identity, and writes to a separate output root. **Product separation (D-007) is honoured in substance:** the products — content, images, finished books, folders — never touch; only the neutral build machinery is common. The frozen renderer core is never modified for support-book needs (support-book code lives in its own adapter that spawns the renderer's scripts); a core change is validated against both products before shipping. Assembly process specified in `ASSEMBLY.md`. | Keeps the font/script-guard/geometry discipline singular (drift would print tofu); extends the seam the storybook codebase already draws between frozen renderer and per-product workbench. | Tooling, assembly, D-007 | ACTIVE | Principal |
| **D-018** | 2026-07 | **Governance is six files** (amends D-015): README, DECISIONS, SCHEMA, REF-1, REF-2, **ASSEMBLY**. ASSEMBLY.md is the render-tail spec companion. The renderer itself is not a project file — it is a build tool on the machine (D-017). Production holds working artifacts + tooling: pruned TG/skeletons for in-build books, book JSONs, patches, `letter_inventory_*` files, `refs/school/<class>/` cast images, the validator script, and the support-book render adapter + separate content/output roots. | Records ASSEMBLY.md's addition without editing the append-only D-015. | Structure | ACTIVE | Principal |
| **D-019** | 2026-07 | **Per-book setup is specified in `SETUP.md`** (Governance, seventh file — amends D-018 count to seven). Setup **imports and cleans** the TG + skeleton from the source project: the pedagogical payload (শিখনফল/যোগ্যতা codes, corrected genre tags, letter-sequence data, tier notes) arrives **final and is read-only**; cleaning removes/re-points old-flow framing and foreign decision-series references to prevent **chat contamination** and gives the file Production citizenship (original stays archived in the source project). Verification = **codes-count reconciliation** against the retained original. SETUP.md also carries the cast-ref generate-if-absent rules. | The prune step was mentioned but never specified and had never run; its real risk is misleading a chat with retired instructions, not cosmetics. | Setup, structure | ACTIVE | Principal |
| **D-020** | 2026-07 | **Mixed-classroom fiqh (pre-puberty free-mixing) — S4, raised at পাঠ ৩, OPEN, referred to the আলিম lane; no content applies it until ruled.** While OPEN, lessons stay on the compliant default (পাঠ ৩ kept all-boys). *Reconstructed from in-book citations — Principal re-approved.* Sources verified at source before writing: **L003** note ("user (Principal) raised a pre-puberty free-mixing position for mixed cast scenes; NOT applied here — flagged as S4 governance/আলিম question, পাঠ ৩ kept compliant all-boys pending any recorded DECISIONS.md amendment"), **L004** note ("Pre-puberty mixed-class governance question remains OPEN/unapplied (S4)"), **L006** note (same). | A governance/fiqh question the agent may not settle; the safe default holds until the reviewer lane rules. | C1-BAN L03, L04, L06; all future mixed-cast scenes | **OPEN — আলিম lane (RQ-003)** | Principal (re-approved 2026-08-09) |
| **D-021** | 2026-07 | **Weapon-line ruling (পাঠ ২৩, ঢ sentence).** NCTB p.33 reads *'ঢাল তলোয়ার ঝনঝনিয়ে বাজে।'* — combat/clash framing, flagged **S4 (no C-code)**. Principal-resolved by replacing it with a static wall-display line: *'ঢাল তলোয়ার দেয়ালে সাজানো।'* Establishes the **weapon-as-object precedent**: a weapon may be retained as a still object — no combat, no aiming or throwing, no figure wielding it — and is applied at পাঠ ২৪ (ধনুক, img-04). *Reconstructed from in-book citations — Principal re-approved.* Sources: 9 citations in `support-book_C1-BAN.json` (L23 compliance entry + L24 image slots and compliance notes). | Combat framing is S4; a static object is not. | C1-BAN L23, L24; weapon-as-object precedent | ACTIVE | Principal |

