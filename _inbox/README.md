# `_inbox/` — staging (contents gitignored; this note is not)

Drop source files here; the agent normalises and slots them. **Nothing here is authority.**

**This README is committed, the rest of the folder is not** (`.gitignore`, CD-128 — the same form
`_unvendored/README.md` uses under CD-126). The staged bytes stay per-machine; the *accounting*
travels, so a session on another device can read what is here and why without being able to see it.

---

## Retained by ruling — do NOT re-raise on a retention sweep

**CD-128 (Principal, 2026-08-14), closing `PENDING-P-032`.**

| File | Why it stays | Owner | Until |
|---|---|---|---|
| `Class 5 Bangla.pdf` | **Scan-of-record** for the C5 Bangla book — the authority any spot-check or re-extraction is run against. Step ① is closed, but পাঠ ১২ extraction is now **permitted on the Principal's call** (CD-127), so the book's own scan has a named future consumer. | Principal | **Indefinitely** |
| `Class 5 English.pdf` | **Scan-of-record** for the C5 English book. Step ① closed; retained for spot-check and any re-extraction. | Principal | **Indefinitely** |
| `Class 5 Math.pdf` | **Scan-of-record**, and the Math lane has **live extraction work outstanding** — অধ্যায় ৬ is mid-chapter (`নির্মাণাধীন`), ৭–১০ authorised and not begun. | Principal | **Indefinitely** |
| `C5_Science.pdf` | **Scan-of-record**, and the Science lane **has not begun** — its extraction reads this file. | Principal | **Indefinitely** |

**These four are §12.1 row-1 source scans** governed by `SOURCE_POLICY` §2.1/§7.14, outside §12's
reach in class terms. They appeared on three consecutive §12.7 retention lists, which is where the
three-session rule bites: **an owner and a date, or out.** They now have both — **owner: Principal;
retained indefinitely as the scan-of-record** — so the rule is *satisfied*, not deferred a fourth
time. A retention list must still **name** them (§12.7 admits no exemption); it must no longer
**raise** them.

**What would change this:** the Principal saying so. Nothing else — not a closed lane, not a
finished extraction. A scan-of-record outlives the extraction taken from it; that is the whole
point of keeping one.

---

## Everything else here is still owed

Anything in this folder **not** in the table above is unaccounted for and belongs on the next
session's §12.7 retention list with a one-line reason and a named owner.

As of 2026-08-14 that is the four Math OCR drafts —
`C5_MATH_OCRDRAFT_ch7.md` · `ch8.md` · `ch9.md` · `ch10.md` — retained because অধ্যায় ৭–১০ have
**no committed evidence copy** under `canon/_wip/c5-math/evidence/`, so §7.14.3a has not yet run on
them and deleting them would discard the draft, which §7.14.1 forbids. **They leave when their
chapters are extracted and the drafts are committed as evidence.** Owner: Principal (Math lane).
