# canon/language — index

✅ SLOTTED 2026-08-09 (Step 1 canon extract).

| File | What it settles |
|---|---|
| `LANGUAGE_RULES.md` | How the school writes — register, the Bengali/English numeral split, name spelling, সাধু vs চলিত, replacement preservation |

Consolidated from AGENTS.md §7, the MarkLogic spines, REF-2 §0 and REF-1 §4.2–§4.4, plus the
সাধু/চলিত ruling (CD-008). Consumed by all reader-facing output.
Cite the path; never copy (AGENTS.md §8).

Two items are deliberately **not** here: the Hub renderer script guard (deferred to Step 2,
lands with the LOCKED import contract) and the swarabritta rhyme spec (out of scope until a
workstream writes verse). See `LANGUAGE_RULES.md` §7.
