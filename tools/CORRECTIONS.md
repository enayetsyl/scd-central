# CORRECTIONS.md — tools lane ledger (append-only, AGENTS.md §6)

<!-- CD-124: the prefix is what rows in this file ALREADY mint; the lane identifies this ledger. Declared now, renumbered per lane at close — no row is renumbered by this line. -->
<!-- ledger-prefix: TOOLS-CR -->
<!-- ledger-lane: tools -->

Agent errors in repo-wide tooling: gates, harnesses, render and audit scripts. **Rows are never
edited or deleted** — a wrong row is corrected by a new row citing it.
3+ occurrences of one shape → mark **PATTERN** → propose promotion to an executing gate.

**Why this file exists and why it has its own prefix — `TOOLS-CR-###`, starting at 001 (Principal
ruling 2026-08-14, session-2 ruling 4).** Per-lane prefixes are already the pattern
(`QB-CR-###` in question-banks, bare `CR-###` in the Math lane), and **two ledgers sharing one
number space across disposable sessions is CD-034's disease in a new place**. A tooling defect
belongs to no extraction lane; this file is its home, and its rows are numbered from 001 in a
space nothing else writes into.

**The renumber corrected a live collision, which is why it is recorded and not just done.** This
file's first row was filed as **`CR-012`** on the stated ground that *"CR-012 was verified free at
source … no `CR-012` token existed anywhere in the repo."* **That was false at source.**
`canon/_wip/c5-math/CORRECTIONS.md` already carried its own **CR-012** (the pair-general band-crop
rule, 2026-08-12, commit `5634a5f`), and `canon/_wip/c5-math/C5_MATH_Source_06.md` cites it three
times. **Two different rows carried one number for two days, and three live citations pointed at
the Math one.** The Math lane's CR-012 keeps the number — it was written first and it is the one
cited — and **the Math lane is not touched by this correction**; only this file's row is renumbered.

| TOOLS-CR | Date | Where | What was wrong | What was done |
|---|---|---|---|---|
| TOOLS-CR-001 | 2026-08-14 | `tools/audits/canon_check.py` · `parse_ref_manifest()` / `check_ref_citations()` | **The REF-CITE resolver normalised REF ids with `int()`, and `int("01") == int("1")`.** P00's **REF-01** and the retired support-book **REF-1** therefore collapsed to the same key. Because the current-series set was built first, it swallowed the alias set, and the `raw in alias` branch became unreachable. **Effect: every retired support-book citation silently stopped being censused while the gate still printed `CLEAN`.** No FAIL, no WARN, no visible symptom — the check reported success for a population it was no longer looking at. **This is the REF-01/REF-2 collision reappearing inside the gate built to police that exact collision.** | Keys are now the **raw string the register writes**, never integer-normalised; the two series are told apart by the **width** they are written at (P00 zero-padded two-digit `REF-01…REF-26`; retired support-book bare single-digit `REF-1`, `REF-2`), which is the same property that makes them two series. Reason recorded in the function docstring so a later reader does not "tidy" the normaliser back in. **Caught by the seeded census test, which existed only because the census was built with a seed in both directions** — the clean-repo run was green throughout. |

| TOOLS-CR-002 | 2026-08-14 | `tools/hub-export/validate_import.py` · `REF19_SLUGS_DEFAULT` | **A FIFTH instance of the promoted PATTERN, found by building the gate that reads the same register — and this one is in a LOCKED-adjacent constant.** `canon/topics/LOCKED_REF-19_…v1_10.md` carries **121 backticked slugs**, two of which are three-segment: **`MATH-ADDSUB-REL`** and **`MATH-MULDIV-REL`**. The harness's auto-extracted copy also holds 121 — but holds **`MATH-ADDSUB`** and **`MATH-MULDIV`** instead. **The extractor's regex stopped at the second hyphen, and the truncated forms exist nowhere in REF-19.** Set difference, both directions, measured: `artifact − harness = {MATH-ADDSUB-REL, MATH-MULDIV-REL}`, `harness − artifact = {MATH-ADDSUB, MATH-MULDIV}`. **This is `normalising an ID discards the thing that makes it an ID` exactly** (CD-088) — the discarded form is a hyphenated segment, the same class of loss as CD-088(b)'s scheme prefix. **And it compounds:** `LOCKED_QuestionPayload_Schema_v1.json`'s `ref19_topic_id` pattern is `^(BAN\|ENG\|MATH\|SCI\|BGS)-[A-Z0-9]+$` — **single hyphen only** — so the two real slugs are *unrepresentable in a payload*, and the only values that validate are the two truncations that do not exist. Two canon-layer artifacts disagree about what a REF-19 id is. | **Nothing was changed.** The REF19-SLUG gate in the merged `gates.py` (CD-123) reads the **LOCKED artifact**, not the derived copy (CD-011: a registry is written from the artifact, never from a summary — the same ground QB-CR-007 refused to build canon on this constant), so it accepts the two real slugs and would reject the truncations. **The harness is vendored under a LOCKED contract and is supersede-only (CD-013); the schema pattern is LOCKED.** Neither is the agent's to edit and neither was edited. Raised as **⚑ PENDING-P-029** — this is a Principal ruling about which of two canon artifacts is right, and it is upstream of both. |
| TOOLS-CR-006 | 2026-08-16 | `tools/session_bootstrap.md` §2 · §4 — the boundary between the container clone and the pull-only mount | **THE PATTERN, NOT THE INCIDENT: a container-clone session had the pull-only mount both WRITABLE and REACHABLE, and the boundary held only because the agent noticed.** §4 states *no agent writes to `C:\scd-central`* and gives the reason. **What §1–§5 never stated is that the session has TWO file surfaces with DIFFERENT ROOTS, and only one of them can reach the clone at all.** Bash reaches the clone at `~/work/scd-central`; the Read/Write/Edit tools address the HOST path **`C:\scd-central`, which IS the mount** — and they are the natural instrument for editing a file. The whole bootstrap is written in bash, so **an agent following it exactly still writes to the mount the moment it reaches for the editing tool.** The rule was stated; the trap that defeats it was not. **And nothing stops the write.** Measured: the mount is `drwx------` while `/sessions/<s>/mnt/uploads` is `dr-x------` — **the host already mounts read-only where it means to, and the repo folder is not one of those.** FACT 1 blocks unlink inside `.git/` only; ordinary tracked files take writes normally. **OCCURRENCE (one):** an edit landed on `tools/audits/slot_register_check.py` on the mount, was caught on the next action, and was reverted by reverse-edit — never by git, which cannot `reset --hard` there. Verified byte-clean: `git diff` empty, `git status` clean, no token trace, CRLF working-tree form preserved. **The recovery worked; the point of this row is that it was needed at all.** | **(1) CAN THE BOOTSTRAP LEAVE THE MOUNT UNREACHABLE AFTER THE CLONE STEP? NO — measured inside the container.** `umount` returns *must be superuser to unmount*; `chmod a-w` on the mountpoint is a **no-op** (mode reads back `drwx------` unchanged). The mount is established by the host before the container starts and **nothing available to a session can drop it or narrow it.** The fence therefore **cannot be enforced from the bootstrap**. It can only be (i) **DETECTED** in-session, or (ii) **moved to the host** — and (ii) is the real fix and is not the agent's to make: mount the repo folder **`dr-x`, exactly as `uploads` already is.** The bootstrap needs the mount for **two reads only** — clone source, and the PAT from its remote — both of which survive a read-only mount unchanged. **Raised for the Principal as the durable fix.** **(2) TEXT FIX APPLIED, same commit:** `session_bootstrap.md` §2 now opens with the two-root hazard in its own words — a surface/root table, the fact that the file tools cannot reach the clone at all, and the no-git revert procedure. **A rule that names the trap is not the same as a rule that assumes nobody falls in it.** **(3) THE CHECK — a surface EXISTS and is named; NOT built here.** It must not use git: running `git status` on the mount is precisely what leaves the FACT 1 lock. The surface is a **pure-stat manifest** (relative path · size · mtime · mode) taken over the mount immediately after the clone step — which already walks the whole tree — and re-verified at session close. No git, no writes, and it detects a write even after it has been reverted. Proposed as `tools/audits/mount_fence.py --snapshot` / `--verify`, seeded both directions: a touched tracked file FAILs, a new untracked file FAILs, an untouched tree stays quiet. **Deliberately not written:** a new tools-lane file carries `tools/MANIFEST.md` and `SMOKE.md` obligations and `tools_check.py` already warns `[VENDORED-UNPROVEN]` on one script — **closing a discipline gap by adding a second unproven tool would be its own defect.** Authorization requested. **(4) NOT CONFLATED, named because it shares a root cause:** the same session also left `.git/index.lock` on the mount by running `git status` there. **That is FACT 1's already-documented pathology and TOOLS-CR-004's clean-up, not this row's defect** — a *read* command leaving debris, where this row is a *write* reaching a file. (The lock has since been cleared; the mount now sits clean at `origin/main`.) They are named together only because both come from one reflex — treating the mount as an ordinary working copy — and because that reflex is exactly why the surface at (3) must avoid git. |

| TOOLS-CR-007 | 2026-08-16 | `tools/audits/slot_register_check.py` · `selftest()` seeds `SUBJECT-BLIND` / `SUBJECT-REFUSAL` | **THE PATTERN, NOT THE INCIDENT: A SEED WHOSE PREMISE THE REPO CAN LATER SATISFY. It then passes by being UNTESTABLE rather than by being true, and nothing says so.** `SUBJECT-BLIND` asserts that a subject declared in `scope_built` with **no rows** must FAIL — the subject-axis form of the C1–C4 repair (*a column that was never read, reported clean*). It made that assertion by mutating the register: `scope_built.append("ENG C1-C5")`. **The mutation was a LIE ABOUT THE REGISTER, and the ENG data commit made the lie TRUE.** From that moment the seed mutated nothing — appending a subject that was already built and populated — so `run_everything` returned the same verdict as the baseline and the seed reported **PASS**. **It did not fail; it stopped being a test, and the PASS line read exactly as it had the day before.** **THE SHAPE, STATED GENERALLY:** a seed is a claim that a specific defect is DETECTED. It is only a test while its mutation actually produces the defect. **When the fixture premise is drawn from the repo's own roadmap — anything in `scope_owed`, any not-yet-built artifact, any 'this does not exist yet' — the premise has an EXPIRY DATE, and the seed dies silently on that date.** This is QB-D-012 / CD-121(e) (*controls may be live, SEEDS may not*) reaching a case those rows did not name: the seed here was synthetic in form but its PREMISE was live. **It recurs every time a subject moves from owed to built**, and more generally every time the repo grows into a fixture's negative space. **Caught only because the ENG data landed in the same session as the seed**, so one run showed `SELFTEST RESULT: FAIL` on it before the amend; had the two been separate sessions the seed would have gone quiet with no run ever showing red. | **(1) FIXED, and NOT by re-pointing at the next real subject.** The obvious repair — move the target from ENG to MATH — **defers the identical death**, because MATH is in `scope_owed` and is scheduled to be built. Both seeds now target subjects the register can NEVER build: `SUBJECT-BLIND` → **`SYN`**, a seed-only `SPINES` entry that is not a subject and is documented as fictional at its definition; `SUBJECT-REFUSAL` → **`ZZZ`**, in no spine map and on no roadmap. `MATH` stays in `SPINES` on its own merits — listed ahead of its rows so they are proven rather than skipped the day they land. **(2) THE AUDIT the Principal ordered — every other seed in the suite checked for the same shape. THREE findings, REPORTED AND NOT FIXED.** **(i) `slot_register_check.py` · the CD-138(b) marker-scrub negative.** It builds `scrubbed` by `spine.replace("*(দুটোর যেকোনো একটা)*", "")` and asserts the verdict is unchanged. **It never asserts the replace changed anything.** If those markers ever leave the BAN spine — and CD-138(b) exists precisely because they are authoring evidence with no machine meaning, so removing them is a live possibility — `scrubbed == spine` and the negative becomes a comparison of a file with itself. **Same shape, one axis over: the premise is 'the markers are present', and the repo can stop satisfying it.** **(ii) `slot_register_check.py` · the SHAPE seed at `BAN-S10 selected="ভাব নির্ণয়"`.** The seed proves an off-set `selected` is caught, using a value chosen for being wrong. **`ভাব নির্ণয়` is the exact value under active consideration in the OPEN question of whether BAN-S10 at C5 should be ভাব নির্ণয় rather than পদ নির্ণয়.** If that is ruled, the seed's mutation becomes a no-op against the admitted set and it goes vacuous — **a fixture premise waiting on a Principal ruling.** **(iii) THE CLASS-WIDE HOLE, which is the useful finding: NO seed in either file asserts that its mutation actually mutated.** Every one of them — `.update({...})`, `.pop()`, `.append()`, `.remove()` — is a no-op if the target already holds the value, and all of them then report PASS. A single guard in the harness (*the mutated register must differ from the original, or the seed is reported HELD rather than PASS*) would close (i), (ii), this row's own defect and every future instance at once — **and the `HELD` machinery to report it already exists**, built for `RowNotBuilt`. **Not built here per the Principal's instruction to report the audit and fix only this one.** `gates.py`'s seeds were checked and are clean on this axis: they run against `_synth_register()`'s fictional পাঠ ৯৯ fixture, whose premise the repo cannot make real. |

| TOOLS-CR-008 | 2026-08-17 | The 5b fix loop — `tools/bank_factual_review_prompt.md` check 1 (FACTUAL ACCURACY) and the sessions that act on it | **THE PATTERN: A REVIEWER NAMES AN ITEM; THE DEFECT IS A CLAIM THAT LIVES IN SEVERAL ITEMS; FIXING THE NAMED QID LEAVES THE DEFECT STANDING ELSEWHERE IN THE SAME BANK.** It happened TWICE in one session, both times to the same agent, and both times the re-run caught it — which is the only reason it is a record and not a live defect. **INSTANCE 1.** The পাঠ ১৩ report flagged `Q71` for keying *চাঁদের আলো সারা রাত ছড়িয়ে পড়ছে*, a claim the section does not make. `Q71` was fixed and its `model_note` was given the bar in words: *আলো … ছড়িয়ে পড়া পাঠে নেই, তাই তা দাবি করা যাবে না*. **`Q75` keyed the same claim and was untouched, so the bank then FORBADE IN ONE ITEM WHAT IT KEYED IN ANOTHER** — strictly worse than before the fix, because the contradiction was now explicit. **INSTANCE 2.** The পাঠ ১৬ report flagged `Q58` for asserting a causal link between the ১৯৪৮ language demand and the ১৯৭১ killings. `Q58` was fixed; **`Q67`'s rubric went on REQUIRING that link for full marks** — *একটির সঙ্গে অন্যটির যোগ দেখানো হয়েছে* — in a `band_descriptors` string, which is a place a reader looking at answer keys does not look. **WHY IT IS A PATTERN AND NOT TWO MISTAKES.** A report is organised by ITEM because that is what a reviewer reads; a factual defect is organised by CLAIM, because a claim is what a bank asserts and one claim can sit in a stem, a key, a `why_wrong` and a rubric band across four qids. **Acting on the report's organisation instead of the defect's is the error, and it is invited by the report's own shape.** A local typo has no siblings and fixing the named qid finishes it; a CLAIM almost always does. | **(1) BOTH INSTANCES FIXED** in `cf7fb68` — `U13 Q75` re-keyed to elements printed in the stanza with `Q71`'s bar attached and named as one rule; `U16 Q67`'s criterion and `সম্পূর্ণ` band now ask only that the three times be told apart. **A THIRD DEFECT WAS ALMOST INTRODUCED WHILE FIXING THE SECOND**: the first `Q67` edit dropped the clause *তারিখগুলোকে ইতিহাসের তথ্য হিসেবে ব্যবহার করা*, which is the **C-18 carve-out guard** holding the answer to factual history rather than veneration. Restored on re-reading the diff. **No gate would have caught it** — C-18 compliance in a rubric is exactly what 5b exists to check and what the structural suite cannot see. **(2) THE REMEDY OF PRACTICE, recorded as the rule for the next session:** when a flagged defect is a **factual claim** rather than a local typo, the fix **sweeps every item touching that claim** — `question_text`, option `text`, `why_wrong`, `answer_key.accepted`, `model_note`, rubric `criterion` and `band_descriptors` — not only the reported qid. Grep the claim, not the id. **(3) THE DURABLE HALF EXISTS ALREADY, IN THE PROMPT, APPLIED TO THE WRONG CHECK — and this is the finding worth keeping.** Check **2** (CONTENT BARS) is exhaustive by construction: *List **every** occurrence with its qid and field.* Check **1** (FACTUAL ACCURACY) is per-item: *for every item, is the answer correct.* **Check 2 produced no recurrence this session and check 1 produced two** — the difference is not reviewer diligence, it is that one check is asked to enumerate and the other is not. **PROPOSED, NOT WRITTEN** (the Principal's instruction): give check 1 the shape check 2 already has — *for each factual claim you flag, list EVERY item that carries it, with its qid and field, and say which are the same claim.* The prompt is vendored under CD-151 and this row does not amend it; a change to the artifact that grades banks is the Principal's. **(4) A SECOND, MINOR INSTANCE OF TOOLS-CR-007(iii)'s CLASS, reported not fixed:** `gates.py:2931`'s `LIVE-CONTROL` label prints *margins +12 · +2 · +2 · +3* as static text while the live margins are now **+11 · +3 · +2 · +2**. The ASSERTION is live and correct — it went red on the retirement and green on the authoring — but its DESCRIPTION is stale, so a reader trusting the line reads four wrong numbers from a passing gate. Same family as the vacuous-seed finding: the check is sound, the sentence about it has drifted. |
| TOOLS-CR-009 | 2026-08-19 | `tools/audits/source_check.py` · `SPINE` dict / `parse_subject()` | **A SPINE KEY THAT NO FILENAME CAN PRODUCE, SO THE GATE REPORTED "NOT REGISTERED" FOR A POPULATION IT WAS SUPPOSED TO JUDGE.** BGS and SCI share one spine, `canon/marklogic/MarkLogic_SCI_BGS_Spine.md`, and `SPINE` was keyed on the combined form `SCI-BGS`. But `parse_subject()` derives its key from the **filename token**, and no source file is named `C5_SCI-BGS_Source_NN.md` — every real file carries `BGS` or `SCI` alone. **The combined key was unreachable by construction**, so SLOTS reported *no spine file registered* on the BGS and SCI extractions while the gate exited clean. **This is TOOLS-CR-001's family** — a check reporting success for a population it was no longer looking at — arriving from the opposite direction: -001 collapsed two keys into one, this splits one spine into a key nothing writes. **SECOND DEFECT, on the process not the code:** the Principal ruled the fix **2026-08-17**; it sat **uncommitted in the working tree for two days** while every suite run reported CLEAN, because `source_check.py` takes a path argument and **is run by nothing in a default pass**. Five of eleven `tools/audits` scripts are in that class. A gate that no entry point invokes cannot go red, and cannot report that it did not run. | **(1) FIXED in `d9afff0`** — both `BGS` and `SCI` resolve to the shared SCI-BGS spine; the combined key is no longer relied on. Verified at source this session: subject reads `BGS` and `SCI` on real filenames, selftest PASS, all seeds still biting. **(2) THIS ROW FILED AFTER THE CODE COMMIT, and that is itself recorded** — `d9afff0` cites the ruling date and no row id. The ledger is append-only, so the row lands late rather than not at all; the ordering defect is named here so it is not read as the convention. **(3) THE DURABLE HALF, NOT BUILT HERE:** `run_all.py --sources` (PLAN Phase A2) is the entry point that would have caught the two-day gap. It **must honour the নির্মাণাধীন skip** as `--selftest` already does — 100+ source files are under construction and a naive walk reddens them permanently, which is CD-152(d)'s named failure. **(4) NEXT-FREE BY BOTH METHODS, per TOOLS-CR-005.** *Ledger read:* this file defines through `TOOLS-CR-008`. *Token sweep:* `findstr /S` **announced two files it could not open** — `C5_BGS_Source_পরিশিষ্ট.md` and `C5_SCI_Source_শব্দকোষ.md`, Bengali-named and outside the code page — so it was re-run in PowerShell over **1,142** `*.md`/`*.py`/`*.json` files with `.git` excluded: **zero `TOOLS-CR-009` tokens**. The two methods agree, which is what -005 asks for and what -005's own Instance 3 lacked. The detectable variant of the shape: the incomplete query said so. |
| TOOLS-CR-010 | 2026-08-19 | `tools/audits/ledger_check.py` · `ROW_ID` (line 78) | **THE LEDGER-COMPLETENESS GATE HAS BEEN OMITTING A ROW SINCE THAT ROW WAS FILED — AND IT IS `TOOLS-CR-005`, THE ROW THAT SAYS A QUERY'S SILENCE IS EVIDENCE ONLY IF THE QUERY IS KNOWN TO BE COMPLETE.** `ROW_ID` admits an id cell wrapped in `**` or backticks and nothing else. `TOOLS-CR-005`'s cell reads `| **TOOLS-CR-005 · PATTERN CANDIDATE** |` — the `·` and the label sit INSIDE the bold, before the closing pipe — so the row does not match and is not counted. **Measured:** the regex run standalone over the file returns 8 ids — `001 · 002 · 006 · 007 · 008 · 009 · 003 · 004` — while the file holds 9 rows. **The count has read 8 both before and after `TOOLS-CR-009` was committed**, which is how it went unnoticed: the new row's +1 and the old row's −1 cancelled, and a number that does not move reads as a number that is right. **WHY THIS IS -005's OWN SHAPE AND NOT MERELY A REGEX BUG.** `TOOLS-CR-005` names the ledger read as the trustworthy method precisely because *a next-free check that reads the LEDGER FILE rather than grepping tokens cannot miss a row the ledger defines*. This one can, and did. It is also the second time a check has been narrower than the claim it tests inside the same file this session (cf. `TOOLS-CR-009`). **The cost was not paid only because `TOOLS-CR-009`'s next-free was run by BOTH methods** and the token sweep — the method -005 calls the weaker one — is what covered the gap. Had the ledger read been trusted alone at a number 005 occupies, the check would have reported free a number that is taken: CD-087(b)'s named worst case, converting a collision into a documented non-collision. | **(1) NOTHING EDITED. `TOOLS-CR-005`'s row is not reformatted** — this ledger's header rule is that rows are never edited, and a row rewritten to satisfy a parser is a rewrite of history to quiet a gate (AGENTS §4). The defect is in the reader, not the row. **(2) THE FIX, PROPOSED AND NOT BUILT — the Principal's to authorise.** Widen `ROW_ID` to accept trailing text inside the id cell (id first, then anything up to the closing pipe), seeded BOTH directions: `**TOOLS-CR-005 · PATTERN CANDIDATE**` must be COUNTED, and a cell whose first token is not an id must still not be. **(3) THE CHECK THE COUNT NEEDED AND DOES NOT HAVE.** A row count is an absolute number nothing cross-reads, so it cannot go red — it can only be wrong quietly. The available cross-read is cheap: the ledger declares its prefix, so **the count should be asserted against the highest ordinal it read** and any gap between them PRINTED. Here that is `high water 009, rows counted 8, gap 1` — one line, and the defect announces itself on every run instead of waiting for someone to add a row and check the arithmetic by hand. **Proposed, not built.** **(4) NEXT-FREE BY BOTH METHODS, per TOOLS-CR-005 and stated because this row is about that rule.** *Ledger read:* the file defines through `TOOLS-CR-009` — read from the FILE, not from the gate's count, which this row establishes is short by one. *Token sweep:* PowerShell over the repo's `*.md`/`*.py`/`*.json` with `.git` excluded returns zero `TOOLS-CR-010` tokens. **(5) BUILT THIS SESSION, and the widening surfaced FIVE previously uncounted rows across THREE ledgers — the blind spot was never local to `TOOLS-CR-005`.** `tools/CORRECTIONS.md` 8→10 · `canon/_wip/c5-math/CORRECTIONS.md` 9→11 · `workstreams/question-banks/CORRECTIONS.md` 15→18. **A sixth cross-lane token also became visible**: `CR-005` in c5-math and support-books, DEFERRED under CD-124 like the four already printed — declared, non-ambiguous, not renumbered. Nothing failed and nothing was renumbered; what changed is that five rows the ledger defines are now rows the ledger's own reader can see. **The high-water line is what makes this legible going forward**: it is a lexical max over the RAW ordinal strings, never `int()`-ed (CD-088(d)(i)), and it is a REPORT — the count is still not a verdict, but it now sits beside a number a reader can check it against. **Seeded both directions:** the `**ID · LABEL**` cell must be COUNTED, and a cell whose first token is not an id must still NOT be, so AGENTS §5.1's mint-vs-cite line survives the widening. Suite CLEAN at 15 cases. **`QB-CR-019` remains free — but it was believed free on a count short by three, and is now read against high water 018.**|

| TOOLS-CR-011 | 2026-08-19 | `tools/run_all.py` · `main()` — the receipt's `git.dirty` field | **A RECEIPT THAT WRITES INTO THE TREE IT INSPECTS CANNOT REPORT THAT TREE CLEAN.** `dirty` was read by `git("status", "--porcelain")` at receipt-assembly time, after the gates had run. Under `--receipt <path under the repo>` the receipt is itself an untracked path, so the field mixes the state the run JUDGED with the artifact the run is about to PRODUCE — and any uncommitted receipt from a prior run keeps every later run dirty. The first v2 receipt (commit `2ecdc68`) records `dirty: true` while its own commit message asserts a clean tree. **The message is wrong and stands as history** (AGENTS §4: corrected forward, never edited). The advisor's commit-then-rerun sequencing exposed it, but the property is structural: no ordering makes the field mean what it says while the receipt lands under `workstreams/question-banks/reports/`, which CD-157 requires. | Principal ruled the remedy 2026-08-19. The status snapshot moves to entry of `main()`, before any gate runs, and is carried into the receipt unchanged, so `dirty` states the tree THE GATES WERE RUN AGAINST. It does NOT claim to exclude a leftover receipt from an earlier uncommitted run — that reads dirty and correctly so. No gate asserts on the field; it is recorded, not judged. |

| TOOLS-CR-012 | 2026-08-19 | `tools/run_all.py` — the runner's own stdout encoding, exposed while building `--sources` | **THE RUNNER GAVE ITS CHILDREN AN ENCODING IT DID NOT GIVE ITSELF.** `run_gate()` has always set `PYTHONIOENCODING=utf-8` in the child env, so every gate printed Bengali correctly — to a console. `run_all.py` itself inherits Windows' cp1252 for stdout, so the moment its output was REDIRECTED (`> file`) or PIPED, `print(r["stdout"])` died with `UnicodeEncodeError` mid-suite. Interactive runs never showed it; the CLEAN verdicts of 2026-08-19 were all read off a console. **A4's pre-push hook runs the suite exactly the way that crashes it**, so a hook built on this would have failed on its first Bengali gate and the cause would have looked like a gate defect rather than an encoding one. Found by redirecting to a file to keep a 126-line source listing out of the console — not by a test. | Fixed forward: `sys.stdout`/`sys.stderr` reconfigured to utf-8 at import, before any gate runs. No environment variable is required of the operator; a fix that must be remembered is not a fix. `errors="replace"` is NOT used on the runner's own stream — a mangled character is a defect to see, not to paper over. |

| TOOLS-CR-013 | 2026-08-19 | Every gate script — `tools/audits/*.py` (10 files) and `workstreams/question-banks/audits/gates.py` — the script's own stdout encoding | **TOOLS-CR-012 FIXED THE RUNNER AND LEFT THE ELEVEN SCRIPTS IT RUNS.** `run_gate()` sets `PYTHONIOENCODING=utf-8` in the CHILD env, so a gate invoked through `run_all.py` prints Bengali correctly. A gate invoked DIRECTLY inherits Windows' cp1252 and dies with `UnicodeEncodeError` on the first Bengali character the moment its output is piped or redirected — `python …\gates.py > out.txt` or `| Select-Object`. **The suite masks it perfectly**: every CLEAN verdict this repo has recorded came through the runner, so the defect is invisible to exactly the path that would report it. **MEASURED COST, and it is the reason this is a row and not a note:** two of the three "tests" of A3's `SCD_ROOT` override died in the pipe and returned **exit 1**, which is `run_all.py`'s FAILED code and which was read as a verdict. **A crashed run and a failed run were indistinguishable to the operator**, and the wrong conclusion was drawn about the code twice before a hand-edit and a `git --no-pager diff` established what the file actually contained. This is the runner's own §7.17 line — *a gate that reached no verdict is not a pass* — arriving one level down, where nothing was enforcing it. **Verified at source before this row: all eleven files, zero `reconfigure` calls, so the omission is uniform and not a gap in a partial rollout.** | Principal ruled (a) 2026-08-19: **fix all eleven**, rather than filing the defect and relying on the runner being the normal entry point. The same three-line reconfigure block TOOLS-CR-012 put in `run_all.py` (lines 38–44) is added at import in each file, with `except Exception: pass` so a stream that cannot be reconfigured is not a crash. **`errors="replace"` is deliberately NOT used**, for TOOLS-CR-012's reason: a mangled character is a defect to see, not to paper over. **No environment variable is required of the operator** — a fix that must be remembered is not a fix, which is the whole argument against option (b). The child-env export in `run_gate()` is left in place: it is now redundant for these eleven and still correct for anything else the runner may call. Also folded in: `SETUP.md` §6's verification line, written in bash form (`SCD_ROOT=/tmp git push`) on a repo whose operator uses PowerShell — a check aimed at a machine that cannot run it, which is the shape §3.1 of PLAN_2026-08-19 exists to warn about, appearing inside the document that carries the warning. |

| TOOLS-CR-014 | 2026-08-19 | `tools/CORRECTIONS.md` — TOOLS-CR-013's scope sentence, amended forward | **TOOLS-CR-013 OVERSTATED ITS OWN CLASS, AND THE FIRST TEST OF IT WAS INVALID.** TOOLS-CR-013 describes eleven scripts dying on Bengali under a pipe. **Measured: `ledger_check.py` redirected returns exit 0** — its output is ASCII apart from `·` (U+00B7) and `—` (U+2014), both of which cp1252 encodes, so it never reaches a character that could fail. It was chosen as the UNPATCHED CONTROL for the fix, and its survival was briefly read as DISPROVING the defect. **The control could not exercise the claim it was controlling for** — the family's shape (QB-CR-017 · CD-166(c) · CD-168 · CD-170(e) · CD-171(e) · CD-172(e)), this time in a TEST rather than in a gate, and caught by inspecting WHICH characters the surviving output contained rather than by trusting its exit code. **Compounding it, a second wrong inference off the same output:** `sys.stdout.errors` reads `surrogateescape` under redirect, and that was momentarily taken as evidence of a silent degradation — characters being replaced rather than raising. No degradation was occurring. `surrogateescape` does not rescue a real Bengali codepoint; the mangling on screen was `Get-Content -Encoding UTF8` reading cp1252 bytes, which is the console-reading artefact this repo's own handoff already documents as *not corruption in the file*. | **THE FIX IS UNCHANGED AND ALL ELEVEN ARE PATCHED.** A script whose output cannot currently fail is one refactor away from printing a Bengali slot label, and a uniform block is cheaper than a per-file judgement about which outputs happen to be ASCII today. **What is corrected, forward, is TOOLS-CR-013's SCOPE CLAIM:** the defect is LATENT in all eleven and DEMONSTRATED in one. `workstreams/question-banks/audits/gates.py`, unpatched and redirected: `UnicodeEncodeError: 'charmap' codec can't encode characters in position 40-42`, **exit 1** — which is `run_all.py`'s FAILED code, and is precisely why two crashed A3 tests were read as verdicts. The same file patched, same command: `RESULT: CLEAN`, exit 0. **The valid control is a script that actually prints Bengali; `ledger_check.py` is not one, and naming it as the control is what this row corrects.** **AND THE ROW'S OWN ID WAS THE THIRD ERROR OF THE SEQUENCE.** This amendment was first filed as `TOOLS-CR-013(a)`, borrowing `canon/DECISIONS.md`'s lettered-sub-clause convention for an ID CELL. `ledger_check`'s `ROW_ID` reads the FIRST TOKEN of the cell, so the ledger minted `TOOLS-CR-013` twice and `DUPLICATE-IN-FILE` FAILed. **Caught by the pre-push hook built forty minutes earlier — its first encounter with a real defect, and it blocked the push** (CD-176). Lettered clauses live INSIDE a row's text (CD-172(a)–(g) is the pattern); **a row's id is one token, and an amendment gets its own number.** |

| TOOLS-CR-015 | 2026-08-19 | `tools/audits/int_id_check.py` — its verdict on a file `ast.parse` cannot read | **AN ENVIRONMENT DEFECT WAS REPORTED AS A CODE DEFECT, AND THE SAME COMMIT PRODUCED TWO VERDICTS.** With `core.symlinks=false` — the Windows default without Developer Mode or admin — git writes a symlinked file's TARGET PATH as its content. `workstreams/lesson-plans/audits/render_plan.py` materialised as a 36-byte text file containing `../../../tools/render/render_plan.py`. `ast.parse` failed at line 1 and the gate went **red on Windows, green on Linux, at one commit**. The red says the code is wrong; the code is fine and the checkout is wrong, and nothing in the output distinguishes the two. **The wider cost is already recorded in PLAN_2026-08-19 §3.1:** `validate_plan.py` imports `render_plan` and globs for `LOCKED_C5_PlanSchema_v1.json`, both stubs on this machine, so it **could not execute at all** — and every TODO note instructing a re-run of it locally pointed at a fallback that could not run, with nothing saying so. This is the ledger's recurring family — a check aimed at a narrower surface than the claim it tests — arriving as a check aimed at a **machine that could not run it**. | **PROPOSED, NOT BUILT — the Principal's to authorise.** An unparseable file should **REFUSE, not FAIL**. `SOURCE_POLICY` §7.17 already draws the line — *a gate that reached no verdict is not a pass* — and REFUSED is the state that says no verdict was reached; `run_all.py` already treats exit 2 as blocking, so a refusal does not weaken the hook (CD-176). The refusal text must name the likely cause (`core.symlinks`) rather than print a bare traceback, since the operator's next action differs entirely between the two readings. **Seed both directions:** a symlink stub must REFUSE, and a file with genuine invalid syntax must still FAIL — or the refusal becomes a way for real defects to leave the suite green. **NEXT-FREE BY BOTH METHODS, per TOOLS-CR-005.** *Ledger read:* this file defines through `TOOLS-CR-014`. *Token sweep:* PowerShell over the repo's `*.md`/`*.py`/`*.json` with `.git` excluded returns zero `TOOLS-CR-015` tokens. |

| TOOLS-CR-016 | 2026-08-19 | Three docstrings — `tools/audits/int_id_check.py` · `tools/audits/source_check.py` · `tools/audits/source_textcheck.py` | **THREE `SyntaxWarning: invalid escape sequence` LINES PRINT ON EVERY SUITE RUN, AND THE LINE PYTHON NAMES IS NOT THE LINE TO EDIT.** Warnings fire at `int_id_check.py:97` (`\d`), `source_check.py:193` (`` \` ``), `source_textcheck.py:19` (`\R`). **But two of the three are MODULE docstrings whose opening `"""` sits at line 2**, far above the offending character — Python reports where the escape is, not where the string starts. `source_check.py` is the exception: its opener and its warning are both at 193, a function docstring on `spine_labels`. **Prefixing at the warning line would be wrong in two of three cases** and would leave the warning in place while looking like a fix. Line numbers re-confirmed current at `ac38a68` and printed again on the `f010dcb` suite run. Cosmetic under the current interpreter; **a hard error in a future Python**, at which point three gate scripts stop importing at once. | **PROPOSED, NOT BUILT.** Add the `r` prefix at each docstring's OPENER — line 2, line 193, line 2 respectively — not at the warning line. Confirm by running the suite and observing the three lines gone; a fix whose only evidence is that the edit was made is not confirmed. **Re-read the line numbers at source immediately before editing**: this ledger's recurring defect is a count carried forward past the commits that moved it (cf. `SETUP.md` recorded as 49 lines when it was 93), and these three have already been restated across two handoffs. **NEXT-FREE BY BOTH METHODS:** *ledger read* — defines through `TOOLS-CR-015`, minted this session; *token sweep* — zero `TOOLS-CR-016` tokens repo-wide. |

| TOOLS-CR-017 | 2026-08-19 | `tools/audits/source_check.py` — no argument parsing | **`--help` CRASHES WITH A `FileNotFoundError` TRACEBACK INSTEAD OF REFUSING.** The script has no argparse; an unrecognised argument is not rejected, it is carried into a path operation that then fails on a file that was never meant to exist. **A traceback is not a refusal** — it reaches no verdict and says nothing about what the script accepts, which is the same §7.17 line `TOOLS-CR-015` turns on, arriving through the argument surface rather than the file surface. Relevant now because PLAN_2026-08-19 §4's A2 pass invokes `tools/audits` scripts by path with arguments, and **five of eleven scripts take a path argument while being run by nothing** — the condition `TOOLS-CR-009` records, under which a Principal-ruled `source_check` fix sat uncommitted for two days while every suite run reported CLEAN. | **PROPOSED, NOT BUILT.** Add argparse with a real `--help`, and make an unrecognised argument REFUSE by name. Scope is this file only; the other four path-taking scripts are named as an open question, not fixed here — **and whether they share this defect is unmeasured, so nothing in this row asserts that they do.** **NEXT-FREE BY BOTH METHODS:** *ledger read* — defines through `TOOLS-CR-016`, minted this session; *token sweep* — zero `TOOLS-CR-017` tokens repo-wide. |

| TOOLS-CR-018 | 2026-08-19 | `workstreams/question-banks/audits/gates.py` · `g_envelope_sync` — the REPORT line on the no-export branch | **THE GATE PRINTS AN EQUALITY THAT IS FALSE, ON THE ONE PATH WHERE A READER MOST NEEDS THE TRUTH.** Run against a freshly authored bank with nothing exported yet, `ENVELOPE-SYNC` printed: `export in sync: 0 envelope(s) == 80 bank item(s), ids and payloads, array and single/`. **Zero does not equal eighty.** Measured live on `C5_BAN_U20_QuestionBank_v1.json` before its export was built. **THE VERDICT IS CORRECT AND THE SENTENCE IS NOT, WHICH IS THE HARDER HALF.** The branch is REPORT-not-FAIL by design and the docstring argues it well — an unrun step is not drift, and failing there would fire on every bank exported before contract v1.1 (`SOURCE_POLICY` §7.17). Nothing about the PASS is wrong. What is wrong is that the line a reader scans on a receipt says *in sync* beside a bank that has never been fanned out, and `in sync` is exactly the phrase that stops a reader looking further. **This is the family this ledger keeps recording from the other side:** usually a check narrower than the claim it tests; here a REPORT wider than the fact it holds. Both end the same way — a reader concludes something the run did not establish. **Relevant now because the chat BUILD lane authors a bank before exporting it (CD-177), so this branch is on the normal path for every future chapter, not an edge case.** | **PROPOSED, NOT BUILT — the Principal's to authorise.** The zero-export path prints its own line naming its own state — that no export exists yet and the comparison was therefore not made — and the `N == M` sentence is reserved for a comparison that actually holds. The `.batch.json` absence already has such a line and reads correctly; this is the same treatment for the envelopes themselves. **Seed both directions:** a bank with no export must print the not-compared line and must NOT print `in sync`, and a bank with a real in-sync export must still print `in sync` — or the fix trades a false green for a gate that no longer says anything on the healthy case, which is the gate an author learns to ignore. **NEXT-FREE BY BOTH METHODS, per TOOLS-CR-005, re-taken at `dd7077e` immediately before filing (CD-154).** *Ledger read:* this file defines through `TOOLS-CR-017`; `ledger_check` independently reports rows 17, high water 017, no gap. *Token sweep:* zero `TOOLS-CR-018` tokens across the repo's `*.md`/`*.py`/`*.json` with `.git` excluded. |

| TOOLS-CR-019 | 2026-08-19 | `workstreams/question-banks/authoring/build_batch.py` — no argument parsing | **`--help` CRASHES WITH A `FileNotFoundError` TRACEBACK INSTEAD OF REFUSING — TOOLS-CR-017's DEFECT IN A SECOND FILE.** `main()` reads `sys.argv[1]` straight into `Path(...).read_text()`, so an unrecognised argument is not rejected; it is treated as a filename and fails on a file named `--help` that was never meant to exist. Measured: the traceback terminates in `pathlib.py … FileNotFoundError: [Errno 2] No such file or directory: '--help'`. **A traceback is not a refusal** — it reaches no verdict and tells the operator nothing about what the script accepts, which is the §7.17 line `TOOLS-CR-015` and `TOOLS-CR-017` both turn on, arriving through the argument surface a third time. **WHY THE SECOND INSTANCE IS WORTH ITS OWN ROW: one occurrence is a quirk of one script, two in different lanes is a property of how scripts in this repo are written.** `TOOLS-CR-017` sits in `tools/audits/`; this one is in `workstreams/question-banks/authoring/`, written by a different pass for a different purpose, and it has the same shape. The script does have a `usage:` string for the zero-argument case, which makes the omission legible as an oversight rather than a decision. **Found by running `--help` to learn the interface during the পাঠ ২০ export, not by a test.** | **PROPOSED, NOT BUILT.** Add argparse with a real `--help`, and make an unrecognised argument REFUSE by name rather than be read as a path. **SCOPE IS THIS FILE ONLY. Whether the other scripts under `workstreams/question-banks/authoring/` share the defect is UNMEASURED, and nothing in this row asserts that they do** — the same restraint `TOOLS-CR-017` used on the four other path-taking audit scripts. If the Principal wants the class measured rather than sampled, that is a separate pass and should produce its own row. **NEXT-FREE BY BOTH METHODS, re-taken at `dd7077e` immediately before filing (CD-154).** *Ledger read:* defines through `TOOLS-CR-018`, minted this session. *Token sweep:* zero `TOOLS-CR-019` tokens repo-wide. |

| TOOLS-CR-020 | 2026-08-19 | `tools/hub-export/build_question_envelopes.py` — the `--envelope-schema` default | **THE DEFAULT POINTS AT A PATH THAT DOES NOT EXIST IN THIS REPO, SO EVERY EXPORT MUST PASS THE FLAG OR FAIL.** Invoked without `--envelope-schema` the script prints `BUILD-QUESTION-ENVELOPES ERROR: envelope schema not found at C:\scd-central\docs\import-contract.schema.json (pass --envelope-schema)` and writes nothing. **Verified at source:** `find` over the repo returns exactly one `import-contract.schema.json`, at `tools/hub-export/import-contract.schema.json`. There is no `docs/` directory holding it. **THE SCRIPT'S BEHAVIOUR IS CORRECT AND THAT IS WHY THIS IS SMALL** — it refuses by name, prints the path it wanted, and names the flag that fixes it, which is more than either of the two rows above manage. Nothing is silently wrong and no artifact was produced from a bad schema. **What it costs is that the documented invocation is not the working one**, and the working one is only discoverable by running the broken one first — which is what happened during the পাঠ ২০ export, and cost one failed run before the flag was supplied. **THE FAMILY IS THIS LEDGER'S MOST FREQUENT: a cross-reference that outlived the layout it named.** Every prior instance was a §-number or a line count; this one is a DEFAULT ARGUMENT, which is the same defect in executable form — and unlike a stale §-number it cannot be read as still-roughly-right, because a path either resolves or does not. | **PROPOSED, NOT BUILT — and the choice is the Principal's, because the two remedies say different things about where the schema belongs.** (a) Move the default to `tools/hub-export/import-contract.schema.json`, i.e. accept that the schema lives beside the script that reads it. (b) Move the file to `docs/`, i.e. accept that the default records an intended layout the repo never adopted. **(a) is the smaller change and (b) is the one that would need to check every other reader of that schema first — `validate_import.py` at minimum, unread here and therefore not asserted about.** Whichever is taken, the fix is confirmed by running the script with NO `--envelope-schema` and getting envelopes, not by observing that the edit was made. **NEXT-FREE BY BOTH METHODS, re-taken at `dd7077e` immediately before filing (CD-154).** *Ledger read:* defines through `TOOLS-CR-019`, minted this session. *Token sweep:* zero `TOOLS-CR-020` tokens repo-wide. |

| TOOLS-CR-021 | 2026-08-19 | `tools/audits/slot_register_check.py:581` — the `UNSELECTED · no reason` seed | **A SEED THAT FIRED ONLY BECAUSE OF A PROPERTY OF THE LIVE ROW IT MUTATED, AND IT WENT SILENT THE DAY THAT ROW CHANGED LEGITIMATELY.** The seed reads `lambda r: row(r, 5, "BAN-S10").update({"selected": None})` — it deep-copies the LIVE register, nulls C5 `BAN-S10`'s `selected`, and expects `UNSELECTED` to FAIL for want of an `unselected_reason`. **It never sets, and never clears, that key.** It fired for as long as the live row happened not to carry one. CD-181 gives that row a legitimate `unselected_reason`, so the mutated row is now the VALID UNSELECTED state, the check correctly stays quiet, and the seed reports `DID NOT FIRE`. **The gate is right; the seed is wrong.** Measured: `SELFTEST RESULT: FAIL`, `RESULT: FAIL (selftest red — no register verdict is believable)`, exit 1 — and because the file refuses any verdict on a red selftest, **CD-181 cannot be published until this is fixed; the pre-push hook blocks it.** The two neighbouring UNSELECTED seeds are unaffected: the empty-string-reason seed OVERWRITES the key, and the negative SETS it, so both remain state-independent. **THE FAMILY, AND IT IS THIS LEDGER'S MOST FREQUENT ONE FROM A NEW ANGLE.** `TOOLS-CR-014` recorded a CONTROL that could not exercise the claim it controlled for; this is a FAIL-SEED that could not exercise the failure it seeded — and in both cases the instrument read green (or red) for a reason unrelated to the property under test. Here the hidden dependency is on live-file state: a seed built by mutating the live register inherits every unstated assumption about what that register currently contains. **It was caught by a ruling that changed the row, not by any test** — and had CD-181 never been ruled, the seed would have gone on passing without ever testing anything. | **BUILT AND PROVEN 2026-08-19, Principal authorised (Ruling A).** The fix is one clause: the seed must CLEAR the key as well as null the field — `.update({"selected": None})` followed by `.pop("unselected_reason", None)` — so it asserts the absence it claims to test regardless of what the live row holds. **CONFIRMED BY THE SEED FIRING, NOT BY THE EDIT EXISTING — and proven in BOTH DIRECTIONS, which is the half that matters here.** Run against the POST-CD-181 register (row UNSELECTED, reason present) the seed FIRES; run against the PRE-CD-181 register (row selected পদ নির্ণয়, no reason at all) it FIRES TOO. **A one-directional test would have proved only that the seed works now, which is exactly the property the original seed also had.** `SELFTEST RESULT: PASS (28 of 28 seeded failures exercised …)` on both, `RESULT: CLEAN`. **THE WIDER QUESTION IS NOT FIXED HERE AND SHOULD NOT BE FOLDED IN:** every seed in this file mutates a deep copy of the live register, so every one of them may carry a similar unstated dependency. **How many is UNMEASURED, and nothing in this row asserts a number** — the same restraint `TOOLS-CR-017` used on the other path-taking scripts. A pass that audits all twenty-eight seeds for live-state dependence is a separate task and should produce its own row. **NEXT-FREE BY BOTH METHODS, re-taken at `dd7077e` (CD-154).** *Ledger read:* this file defines through `TOOLS-CR-020`, minted this session. *Token sweep:* zero `TOOLS-CR-021` tokens repo-wide. |

| TOOLS-CR-022 | 2026-08-19 | `tools/audits/source_check.py` — the walked tree vs. the extractions banks actually cite | **THE `SOURCES` GATE WALKS `canon/sources/**` ONLY, AND EVERY C5 BANGLA BANK CITES AN EXTRACTION THAT LIVES IN `canon/marklogic/` — OUTSIDE IT.** `C5_BAN_U20_QuestionBank_v1.json` declares `extraction_path: canon/marklogic/C5_Bangla_Source_13-23.md`, and `SOURCE-TRACE` resolves all 80 anchors against that file. But `source_check` never opens it. Measured at `dd7077e`: `SOURCES: 31 green · 0 pending · 0 fail · 92 held out · 3 exempt · 126 total` — and `C5_Bangla_Source_13-23.md` is in none of those 126. **SO THE GREEN `SOURCES` LINE ON EVERY U13–U20 RUN HAS NEVER COVERED THE FILE THOSE BANKS ARE BUILT FROM.** The eleven `canon/sources/c5/bangla/C5_BAN_Source_01–11.md` files that ARE walked and green are a DIFFERENT set of extractions, for পাঠ ১–১১, which no published bank cites. **Nothing is wrong with either file and no bank is invalidated** — `SOURCE-TRACE` does read the real extraction, so anchors are genuinely verified. What is missing is §5 conformance: the সই-ছক, the ⛔ উৎস-সীমা block, the পৃষ্ঠা-অফসেট — the things `source_check` exists to enforce — have never been checked on the file eight banks depend on. **THIS IS THE LEDGER'S NAMED FAMILY IN ITS PUREST FORM: a check aimed at a narrower surface than the claim it is read as supporting.** A reader seeing `SOURCES: … 0 fail` beside a U20 verdict concludes U20's source was checked. It was not. Found by REVIEW, which asked what the green line covered rather than accepting that it was green. | **PROPOSED, NOT BUILT, AND THE FILE SHOULD NOT BE MOVED TO QUIET THIS.** Moving `C5_Bangla_Source_13-23.md` into `canon/sources/` would put a file that has never been §5-audited into a walked tree, and it would go red on its first run — reddening a lane to fix a reporting gap, which CD-152(d) names as how a permanently red gate gets waved through. **UNMEASURED AND NOT ASSERTED HERE: whether `C5_Bangla_Source_13-23.md` would PASS §5 if walked.** Nobody has checked; this row claims only that it is not checked now. Two candidate remedies, the Principal's to choose: (a) `run_all.py --sources` also walks the extractions named by every bank's `extraction_path`, so coverage follows the citation rather than the directory; or (b) the `SOURCES` line names its own scope on every run, so a reader can see what the verdict does not cover. **PRINCIPAL RULED 2026-08-19: NEITHER — and the ruling is better than both candidates.** The file is NOT moved. Fresh per-chapter extractions will be authored into `canon/sources/c5/bangla/` under the §5 schema and recorded properly, and the banks re-pointed to them. **This dissolves the defect at its root rather than widening a walk or annotating a verdict:** the reason `canon/marklogic/` holds an extraction at all is that it predates the `canon/sources/` schema, and both candidate remedies would have preserved that split permanently. **UNTIL THOSE FILES EXIST THE GAP IS OPEN and the `SOURCES` line still does not cover what U13–U20 cite** — this row stays open and is discharged by the new extractions passing, not by the ruling being made. **Do the move as its own commit:** a red first run on a never-audited file must read as *this file has never been §5-checked*, not as *the U20 corrections broke something*. **NEXT-FREE BY BOTH METHODS, re-taken at `dd7077e` (CD-154).** *Ledger read:* this file defines through `TOOLS-CR-021`. *Token sweep:* zero `TOOLS-CR-022` tokens repo-wide. **—— MEASURED 2026-08-20, NOT YET DISCHARGED. ——** **THE RE-POINT COST IS NOW A NUMBER RATHER THAN AN ESTIMATE.** `C5_BAN_U20_QuestionBank_v1.json` was copied to a scratch file BESIDE the live bank (never the bank itself), its `extraction_path` repointed to `canon/sources/c5/bangla/C5_BAN_Source_20.md`, and the full suite run: **75 failures, ALL OF THEM `SOURCE-TRACE`.** **Every other gate PASSES against the per-chapter extraction** — `COVERAGE`, `SOURCE-EXCLUSION`, `MARK-VALUE`, `PLAN` and the rest — so **the new extraction is fit for purpose and only the anchors are stale.** That is the fact this row needed and did not have. **AND 75 FAILURES ARE FIVE DISTINCT ANCHORS, NOT 75 PROBLEMS.** Classified by grouping the failure texts rather than by reading a sample: **36** · the whole `অর্থ জেনে নই` glossary block used as one anchor · **29** · multi-line VERSE blocks carrying `>` markdown prefixes · **7** · the exercise heading `**৩.** যুক্তবর্ণ ভেঙে দুটি করে শব্দ` · **2** · `প্রথম ৮ পঙ্‌ক্তি (S01-এর জন্য)` · **1** · `**কাজী কাদের নেওয়াজ · কবিতা (সাধু ভাষা)**`. **The work is PER-ANCHOR, and it is five rewrites.** **72 ARE FORMATTING. THREE ARE NOT, AND THEY ARE THE SERIOUS HALF.** The last two anchors are **LEGACY LABELS THAT WERE NEVER BOOK TEXT** — editorial scaffolding in `canon/marklogic/C5_Bangla_Source_13-23.md`. **CD-191(c) found the same string mechanically across পাঠ ১৩ · ১৮ · ২০ on the same day, by a different instrument.** Those three items have been resolving `SOURCE-TRACE` against a LABEL rather than against the book, which is **this row's own defect one level down**: not a check aimed at the wrong directory, but an anchor aimed at the wrong kind of text. They cannot be re-derived mechanically — each needs an authoring decision about what the item should actually cite. **THE COUNT MOVED, AND THE MOVEMENT IS EVIDENCE.** CD-189 measured **80 of 80** failing on the same re-point before its four spelling corrections landed; it is **75** now. The five closed are the glossary items the `অঞ্জুলি` correction reached. **Re-measured at source rather than carried from the handoff**, because two figures inherited from that handoff were already wrong today (CD-190(d), CD-191). **NOT DONE THIS SESSION, DELIBERATELY.** The change touches a **teacher-signed bank whose re-sign is already PENDING** (পাঠ ২০, Sajeda Jannat, after the CD-189 correction), and three of the five anchors need a Principal-level authoring decision. **This row's own instruction governs: do the move as its own commit**, so a red first run reads as *these anchors are stale*, not as *something else broke*. The scratch file was deleted and `git status` printed clean. **—— ALL EIGHT MEASURED, SAME SESSION. ——** **293 FAILURES ACROSS 86 DISTINCT ANCHORS.** Every bank was copied to a scratch file beside itself, re-pointed to its own `canon/sources/c5/bangla/C5_BAN_Source_NN.md`, run through the full suite, and the failures grouped BY ANCHOR. No live bank was modified; every scratch file was removed and `git status` printed clean. Per bank — **১৩: 4 failures / 3 anchors · ১৪: 45 / 24 · ১৫: 26 / 10 · ১৬: 67 / 19 · ১৭: 28 / 12 · ১৮: 10 / 3 · ১৯: 38 / 6 · ২০: 75 / 9.** Anchor totals are PER BANK and are **not de-duplicated across banks** — whether two banks cite the same string is UNMEASURED, and collapsing them would understate the work if they differ by a character. **THE SPLIT CONFIRMS CD-191 MECHANICALLY, FROM A SECOND AND INDEPENDENT DIRECTION.** The four chapters CD-191 found to have NO verbatim text in the legacy file — পাঠ ১৪ · ১৬ · ১৭ · ১৯ — carry **61 of the 86 anchors**, and their anchors **are not quotations at all**. Read at source: *বাঘ কুয়ার জলে নিজের ছায়া দেখে* · *পশুদের জনসভা সভাপতি মোষ* · *১৯৭১ সালের ২৫শে মার্চ গভীর রাতে…* — these are **SUMMARY SENTENCES SOMEBODY WROTE**, not book text. CD-191 reached that finding by comparing section structure; this reached it by re-pointing and reading what failed. **Two instruments, two methods, same four chapters.** **THE WORK THEREFORE DIVIDES INTO TWO KINDS, AND ONLY ONE OF THEM IS RE-POINTING.** **(i) ১৩ · ১৫ · ১৮ · ২০ — 25 anchors** against chapters that DO have book text on both channels. Mostly formatting: glossary blocks, multi-line verse carrying `>` prefixes, exercise headings. Every re-derived verse anchor **must carry `⸺` U+2E3A per CD-190**. **(ii) ১৪ · ১৬ · ১৭ · ১৯ — 61 anchors** with **NOTHING TO RE-DERIVE FROM**. Their anchors were never book text, so this is not a formatting fix: it means **authoring new anchors against the per-chapter extraction for the first time, and checking whether each question is supported by the book at all**. That is a different task with a different risk, and calling both of them *re-pointing* would hide it. **A CORRECTION TO THIS ROW'S OWN EARLIER APPEND.** The hand grouping recorded **five** distinct anchors for পাঠ ২০. **It is nine.** The hand pass truncated anchor keys at 45 characters and merged five separate verse blocks into one bucket; the script keys at 60 and separates them. **The earlier figure was wrong and is corrected here rather than left standing** — the failure count (75) and the classification into formatting-versus-legacy-label were both right. **AND THE INSTRUMENT ITSELF WAS WRONG FIRST.** Its anchor regex used a greedy `(.*)` under `re.S`, which matched ONCE across an entire run — first anchor to last — and would have reported **1 distinct anchor for every bank**. It passed two hand-checks because each sample held a single failure, so the fault could not appear. Caught by a synthetic three-failure fixture before any live run. **A test that cannot fail is not a test**, and this is the third instance of that shape in one session (TOOLS-CR-025 carries the other two). **STILL OPEN. The close condition is unchanged — discharged by the re-pointed banks passing, not by this measurement.** |

| TOOLS-CR-023 | 2026-08-20 | `tools/audits/int_id_check.py` · `source_check.py` · `source_textcheck.py` · `tools/hub-export/build_question_envelopes.py` · `build_envelope.py` · `workstreams/question-banks/authoring/build_batch.py` · `workstreams/question-banks/audits/gates.py` — **THE SIX `PROPOSED, NOT BUILT` ROWS ARE NOW BUILT. This row is the record of the build and its proofs; it edits nothing (forward-only).** | **TOOLS-CR-015 — BUILT, PROVEN THREE DIRECTIONS.** An unparseable file now REFUSES (exit 2) rather than FAILs, **but only when the content IS a resolving symlink target.** The row required the second direction and it is the whole of the design: a refusal that also swallowed genuine syntax errors would be a way for real defects to leave the suite green. `is_symlink_stub()` therefore tests *does this content resolve to an existing sibling*, not *is it short and unparseable*. Seeds: (i) a stub REFUSES; (ii) `def f(:` still FAILs as PARSE; (iii) **a path-shaped file whose target does NOT resolve still FAILs** — over-refusing would hide real defects behind an environment excuse, and that third seed is the one neither the row nor the proposal asked for. **LIVE RED PATH RUN, not seeds alone:** `render_plan.py` was replaced with its own 36-byte target text, the gate printed `REFUSE SYMLINK-STUB` naming `core.symlinks` and instructing *do NOT edit the file*, `RESULT: REFUSED`, exit 2; the symlink was then restored and `git status` printed clean. **TOOLS-CR-016 — BUILT.** `r` prefixes added at the three docstring OPENERS: `int_id_check.py:2`, `source_check.py:193`, `source_textcheck.py:2`. **The line numbers were re-read at source immediately before editing, as the row demanded**, and the reading confirms the row's warning: on this interpreter the WARNINGS fire at 2 / 193 / 2, but the row recorded 97 / 193 / 19 from a different Python — **the warning line moves between versions and the opener does not**, which is why the opener is the correct anchor and the fix is interpreter-independent. **Confirmed by compiling each file and counting warnings: 0, 0, 0** — not by observing that the edit was made. **TOOLS-CR-017 — BUILT.** `source_check.py` gains argparse. `--help` prints usage and exits 0; `--nope` is REFUSED by name; **the zero-argument and `--selftest` behaviours are unchanged**, deliberately, because those are what `run_all.py` invokes and this row fixes the argument surface, not the contract. **TOOLS-CR-018 — BUILT, PROVEN BOTH DIRECTIONS.** The `N == M … export in sync` sentence is now reserved for a comparison that actually held; the zero-export branch prints `NOT COMPARED`, names that no envelopes exist, and names the three-command chain that builds them. The verdict is unchanged — REPORT, not FAIL. **Both seeds, because the row named the opposite risk:** a bank with no export must print NOT COMPARED and must NOT print `in sync`; a bank with a real in-sync export must STILL print `in sync`, or the fix trades a false green for a gate that says nothing on the healthy case. **TOOLS-CR-019 — BUILT.** `build_batch.py` gains argparse. `--help` exits 0; no arguments exits **2**, which is the code the hand-rolled branch returned and is preserved rather than silently changed. **The fix was confirmed by REGENERATING the live U20 wrapper and getting a byte-identical file** (`git diff` empty, digest `3484d4f87fdc` unchanged) — the interface moved and the output did not. **TOOLS-CR-020 — BUILT, option (a), Principal ruling 2026-08-20 — AND THE ROW'S SCOPE WAS WRONG.** The row named `build_question_envelopes.py`. **`build_envelope.py:49` carries the identical broken default**, found by grepping every reader of the schema before recommending, and both are fixed. **`docs/` does not exist anywhere in this repo**, which is what settles (a) against (b): (b) would create a directory for one file to satisfy a default nobody ever honoured. Confirmed by the row's own criterion — run with NO `--envelope-schema` and get envelopes: **80 written.** **A THIRD READER IS NAMED AND NOT FIXED:** `validate_import.py` has no default at all and globs `*ImportEnvelope*`, which never matches `import-contract.schema.json`. **That is a different defect, it is UNMEASURED here, and nothing in this row asserts about it** — it wants its own row, on TOOLS-CR-017's restraint. **NEXT-FREE BY BOTH METHODS, taken at `origin/main` = `8fef244` (CD-154).** *Ledger read:* this file defines through `TOOLS-CR-022`. *Token sweep:* zero `TOOLS-CR-023` tokens across `*.md`/`*.py`/`*.json` with `.git` excluded. |
| TOOLS-CR-024 | 2026-08-20 | `workstreams/question-banks/audits/gates.py` . `load_envelope_index` - the RETURN surface | **A TRACEBACK IS NOT A REFUSAL, THIRD INSTANCE - AND THE FIRST ON THE RETURN SURFACE.** The function returns THREE values at `1845` (no bank path) and `1873` (unreadable `.batch.json`), and FOUR at `1848`, `1854`, `1863`, `1884`. The SOLE call site is `1926`, unpacking four. **Measured live, not inferred:** `load_envelope_index(None, {qid})` returned `(None, None, "no bank path in context - the export directory cannot be located")`. Under a four-value unpack that is a `ValueError`, not a report. **The reachable path is `1873`: a corrupt `.batch.json` crashes ENVELOPE-SYNC instead of naming the corruption** - and the corrupt-wrapper case is exactly what the surrounding code was built to report, so the failure lands on the one input the gate exists for. The docstring at `1838` still describes the 3-tuple, so the file documents the wrong contract. **Filed as an INSTANCE of a named family, not as a discovery:** TOOLS-CR-017 and TOOLS-CR-019 both carry this sentence, both on the ARGUMENT surface, and under QB-CR-018 neither establishes anything here - argument, argument, now return. | **PROPOSED, NOT BUILT - the Principal to authorise.** Both short returns gain the missing `batch` slot; the docstring is corrected to the 4-tuple. **Seed both directions:** a no-bank-path call must REPORT its note and not raise, and a healthy call must still return its four values unchanged - or the fix trades a crash for a helper that no longer reports on the sound path. **SCOPE IS THIS FUNCTION ONLY. Whether other `gates.py` helpers carry mixed arity is UNMEASURED and nothing in this row asserts that they do** - TOOLS-CR-017 restraint. **NEXT-FREE BY BOTH METHODS, taken at `origin/main` = `16728f9`, fetched immediately before filing (CD-154 . TOOLS-CR-005).** *Ledger read:* this file defines through `TOOLS-CR-023`; `ledger_check` independently reports 23 rows, high water 023, no gap. *Token sweep:* zero `TOOLS-CR-024` tokens across `*.md`/`*.py`/`*.json` with `.git` excluded, **1244 files read** - the count is printed because a first sweep with `findstr` failed to open two Bengali-named files and returned zero, and a zero from a reader that skipped files is not a check. |
| TOOLS-CR-025 | 2026-08-20 | `tools/audits/channel_diff.py` — **NEW TOOL, BUILT. This row is the record of the build and its proofs; it edits nothing (forward-only).** | **WHAT IT IS.** The two-channel comparison CD-189 made BY HAND, built once with seeds. Compares `canon/marklogic/C5_Bangla_Source_13-23.md`'s `## পূর্ণ পাঠ` against each `C5_BAN_Source_NN.md`'s `## পাঠ্য`, word-set, per chapter. **REPORTS ONLY — it rules on NEITHER channel** (CD-189: neither file is authoritative, the printed page decides) and **edits nothing**. Its measurement is filed as CD-191. **NOT A GATE, DELIBERATELY** — `subject_search.py`'s precedent (CD-188). Registering it is a separate ruling: a comparison whose UNCOMPARABLE state is EXPECTED for seven of eleven chapters would be a permanently non-green gate, and CD-152(d) names what happens to those. **SCOPE IS THESE TWO CHANNELS.** `source_textcheck.py` (CD-047) compares raster transcription against the PDF TEXT LAYER — a DIFFERENT PAIR — and under QB-CR-018 establishes nothing here. Neither tool stands in for the other, and this row asserts nothing about the other's surface. **NINE SEEDS, AND THE THREE THAT ARE LOAD-BEARING:** a summary-only chapter is named **UNCOMPARABLE, not silently clean** — zero differences over a chapter that was never compared is the fault this instrument exists after; an extraction carrying its own EXERCISES reads AGREE, because the legacy body is a SUBSET by construction; and a DECOMPOSED word does NOT report as a difference. **TWO DRAFTS WERE WRONG AND THEIR OWN SEEDS CAUGHT THEM.** (i) `### ছাপা NN` page markers were tokenised as book text — every page number would have reported as extraction-only on EVERY chapter, a wall of false findings on first run. (ii) **THE NFC SEED ASSERTED NOTHING, TWICE**: `পাখি` has no canonical decomposition, and `বড়` looked right but **ড়/ঢ়/য় ARE UNICODE COMPOSITION EXCLUSIONS** — NFC does not unify U+09DC with the ড + ় sequence. Now `ছোট` (ো U+09CB), **asserted before use**, because a seed that cannot fail is not a seed. **A THIRD FAULT WAS CAUGHT BY THE DATA, NOT BY A SEED.** The first live run compared a SUBSET against a SUPERSET — legacy `পূর্ণ পাঠ` is the body alone, extraction `পাঠ্য` is the whole chapter — and listed 161 expected words, burying the seven that mattered. Fixed asymmetrically: LEGACY-ONLY is read and listed, extraction-only is counted and never listed. A NINTH SEED was written from that failure. **THE COMPOSITION-EXCLUSION LIMIT IS NAMED AND NOT CLOSED.** ড়/ঢ়/য় can be stored two ways, render identically, and no normalisation unifies them. Recorded in the tool's own docstring so a reader meets it at the point of use. **Whether this corpus actually carries both forms is UNMEASURED and nothing in this row asserts that it does** — TOOLS-CR-017's restraint. It wants its own pass. **PROVEN BOTH PLATFORMS:** `SELFTEST RESULT: PASS (9 cases)` on Linux 3.12 and Windows 3.13.9, **verdicts matching** — the §3.1 precedent, where one commit gave two verdicts on two machines. `--help` exits 0; `--nope` is REFUSED BY NAME with usage and no traceback (TOOLS-CR-017/019's rule). **NEXT-FREE BY BOTH METHODS, working tree at `d7e12ed` (CD-154 · TOOLS-CR-005).** *Ledger read:* this file defines through `TOOLS-CR-024`; `STATE-CHECK` independently asserts 024 against the ledger's own rows. *Token sweep:* ONE occurrence across `*.md`/`*.py`/`*.json`, 1245 files read — `canon/DECISIONS.md:214`, **CD-191's forward citation of this row, NOT a mint**, confirmed by reading the line. |
| TOOLS-CR-026 | 2026-08-20 | `tools/audits/source_check.py` · `check_depth` — the UNDECLARED branch at line 545 | **THE GATE ASSERTS A CHANNEL COUNT THE FILE NEVER STATED, AND CALLS IT A PASS.** `if SINGLE_CHANNEL not in text: return "PASS", "dual-channel source — §7.4 sampling depth governs, nothing to enforce here"`. **ABSENCE OF A DECLARATION IS NOT A DECLARATION OF TWO CHANNELS.** The verdict is a PASS and the sentence is a POSITIVE CLAIM, which is **TOOLS-CR-018's shape a second time**: a REPORT wider than the fact it holds, on the one path where a reader most needs the truth. **THE THIRD DOOR TO PENDING-P-023, AND THIS ROW IS THE (b) HALF FOR THIS BRANCH ONLY.** P-023 found the switch-off via a WRONG `দুই` declaration and CR-008 corrected that file. This reaches the same silent-off through **ABSENCE**, which no sweep over declarations can see — P-023's own widening states it: *a PASS-because-off is indistinguishable from a PASS-because-clean*. **MEASURED, NOT ESTIMATED.** 138 files under `canon/sources/**`: **12 carry the declaration** (C5 Bangla ০১–১১ and `SOURCE_POLICY.md` itself), **126 do not**. Of the 43 JUDGED files, **11 declare and 32 do not** — C5 Bangla ১২–২৩ and C5 English ০১–২০. **THE TWO GROUPS ARE WRONG IN OPPOSITE DIRECTIONS, WHICH IS THE ARGUMENT FOR ONE BRANCH RATHER THAN TWO.** C5 English IS legitimately dual-channel (CD-046/047: born-digital PDF, real text layer, `source_textcheck.py` runs against it), so the sentence there is **TRUE BUT UNEARNED** — inferred from silence, never read. C5 Bangla ১২–২৩ is the §7.7 book measured at **421 extractable characters across 142 pages**, so the same inference is **FALSE**. **A gate right by luck on twenty files and wrong on twelve is reporting an inference as a reading in both.** **FIX (Principal ruling 2026-08-20, option b):** that branch returns **PENDING**, not PASS, naming its own state: *no `যাচাই-চ্যানেল` declaration — channel count not established, depth not judged (§7.7, PENDING-P-023)*. **Nothing is FAILED and no signed file is touched — but the VERDICT DOES CHANGE, and that is the point: PENDING is the mechanism CD-185 already used for schema B, so an undeclared file cannot reach GREEN while the declaration is owed and the debt is COUNTED AND NAMED in the SOURCES census. A corrected sentence under an unchanged PASS would have left P-023(b) unsatisfied — it asks for a disabled check to be visible IN THE VERDICT. Expected effect on the live census: roughly 11 green · 32 pending where it now reads 38 green · 5 pending, which is not a regression but the debt becoming visible.** The declared path is untouched — enforcement still runs and still prints its row counts. **SEED BOTH DIRECTIONS, because the row names the opposite risk:** an undeclared file must REPORT and must NOT print `dual-channel source`; a file declaring `একক` must STILL reach enforcement and print its `পূর্ণ`/`OCR-corroborated` counts — or the fix trades a false claim for a gate that says nothing on the case it was built for. **WHAT THIS DOES NOT DO, AND WHY — PRINCIPAL RULING 2026-08-20.** It does NOT add declarations to the 32, does NOT give schema B a `গভীরতা` column, and does NOT switch DEPTH on for them. **No teacher-signed sign-off table is altered to satisfy a gate.** Sajeda Jannat signed twelve schema-B tables that carry no depth column, and adding one after signing would put a claim inside her signature that she did not make. The depth claim for those files rests on **§7.7's book-wide ruling**, not on a per-row cell. **PENDING-P-023 STAYS OPEN**; its (a) half — a `দুই` declaration on a book measured to have zero letters in its text layer must be RED, not a switch — is untouched here. **NEXT-FREE BY BOTH METHODS, taken at `origin/main` = `37dddc3`, fetched immediately before filing (CD-154 · TOOLS-CR-005).** *Ledger read:* this file defines through `TOOLS-CR-025`; `ledger_check` independently reports 25 rows, high water 025, no gap. *Token sweep:* zero `TOOLS-CR-026` tokens across `*.md`/`*.py`/`*.json` with `.git` excluded, **1245 files read**. |

## PATTERN candidates

**⚑ PATTERN CANDIDATE — `normalising an ID discards the thing that makes it an ID`.**
Named by the Principal on this file's first row (2026-08-14, then numbered `CR-012`, now
**TOOLS-CR-001**). **PROMOTED TO PATTERN 2026-08-14 — four instances on the record.** The gate is
proposed, not built (session-2 instruction: gates are session 3).

| # | Instance | Shape |
|---|---|---|
| 1 | **QB-CR-008** — `TOP-BAN-C5-11` read off MarkLogic spine slot `S11`. The `S-` and `TOP-` schemes are unrelated and collide at 11 by coincidence. | Two ID spaces treated as one because a *rendering* of them matched |
| 2 | **TOOLS-CR-001** — `REF-01` and `REF-2` collapsed by `int()`. | Two ID spaces treated as one because a *normalisation* of them matched |
| 3 | **CD-034 ⚑** — bare `D-0NN` across two registers: `D-049` appears bare 65× as a master row, `D-038` bare 33× *and* as `D-PROJ03-038` 10×. **The same bare string denotes different rows in different places.** | Two ID spaces treated as one because a *truncation* of them matched — the scheme segment (`PROJ03`) is the distinguishing form, and writing the number bare discards it |
| 4 | **This file's own renumber** — `CR-012` filed here while `canon/_wip/c5-math/CORRECTIONS.md` already held a different `CR-012`, on a verification that said the token was free. | Two ID spaces treated as one because *no prefix distinguished them at all* — the shared bare `CR-###` space is the collapse, made permanent |

**Why instance 3 completes it rather than merely resembling it.** The family was stated as: *the
distinguishing information lived in the **form** of the identifier — its scheme prefix, its
zero-padding — and a step that looked like harmless tidying threw that form away.* `D-PROJ03-038`
written as `D-038` throws away a **scheme prefix**, which is the first item on that list. CD-034
recorded it as a flag on a corpus convention rather than as an agent error, which is exactly why
it was not counted the first time; **the shape does not care who performed the tidying.**

**Instance 4 is the same shape reaching the ledgers themselves**, and it is the one that did
measurable harm: two live rows under one number for two days. It is also the instance CD-034
predicted in as many words.

**Proposed gate on promotion (NOT built this session).** Two checks, because the pattern now has
two faces:
1. **Source lint** — forbid `int()` on any captured id group in `tools/audits/*.py` and
   `workstreams/*/audits/*.py`; require comparison against the raw captured string.
2. **Ledger-collision check** — every corrections ledger declares its prefix in its header, and no
   `<PREFIX>-###` token may resolve to rows in two files. This is the check that would have caught
   instance 4 at the moment it was written, and it is the one the repo does not have.

**The family resemblance, stated so the third instance is recognisable:** in both, the distinguishing
information lived in the **form** of the identifier — its scheme prefix, its zero-padding — and a
step that looked like harmless tidying threw that form away. It is the same shape CD-083 named as the
canon's recurring failure (`CD-070` substring-vs-token, `CD-077` presence-vs-correctness): **a rule
written in a coarser unit than the one it is enforced in passes on paper and leaks in practice.**

**Proposed gate on promotion:** a lint over the audit scripts forbidding `int()` on any captured id
group, requiring comparison against the raw captured string. Not built — a two-instance pattern is
proposed, not promoted (AGENTS.md §6).
| **TOOLS-CR-003** | 2026-08-15 | **`GIT_INDEX_FILE` USED AS A LOCK WORKAROUND LEAVES `.git/index` STALE, AND THE STALE INDEX STAGES THE EXACT INVERSE OF THE COMMIT IT MADE.** *(Next free verified at source: this ledger defines through `TOOLS-CR-002`; no `TOOLS-CR-003` token existed anywhere in the repo.)* **(a) What the agent did.** Blocked by an unremovable `.git/index.lock`, the agent committed with `GIT_INDEX_FILE=/tmp/gitidx`. **Commit `cb248be` landed correctly — 5 files, +228/−19.** **(b) The second-order cost, which the agent did not see and the Principal caught.** `GIT_INDEX_FILE` **redirects the index; it does not update `.git/index`.** So after the commit, HEAD contained the five changed files and **`.git/index` still described the pre-commit tree**. The next ordinary `git add`/`git commit` would have staged **`−228/+19` across the same five files — the EXACT INVERSE of `cb248be`.** Verified by the Principal before and after: `git show --stat --format= cb248be` → 5 files +228/−19; `git diff --cached --stat` → the same 5 files −228/+19; `git reset` → clean. **(c) What it would have cost, stated because it is the reason this row exists.** The revert would have removed **CD-133 · CD-134 · CD-135 · CD-136**, returned `QUESTION_POLICY` to **v1.1**, and returned **BLOOM-BAND to a band**. **NO GATE WOULD HAVE CAUGHT IT** — and that is the load-bearing part: **the pre-change rules are internally consistent.** `canon_check` would find every citation resolving, `gates.py`'s selftest would pass against the restored symmetric check, and the suite would print CLEAN on a repo that had silently un-ruled four decisions. **A gate suite proves the state it is run against is coherent; it cannot prove that state is the one you meant.** This is `CD-083`'s shape once more — a rule (or here, a check) written in a finer unit than the thing it protects. **(d) THE RULE, executable: a commit made with `GIT_INDEX_FILE` MUST be followed by `git reset` before any further staging.** `git reset` re-syncs `.git/index` from HEAD and is the whole fix. **(e) And the workaround should not have been reached for at all.** **`AGENTS.md` §9 already documents the approved standing practice** — *"Sandbox cannot unlink inside `.git/` → rename lock files aside"*, to `.git/lock-debris/` per CD-056, and *"move the lock aside immediately before each git write, not once at the start of the session."* **The agent used an undocumented alternative instead of the documented one, and the undocumented one carried a hazard the documented one does not.** §9's practice leaves `.git/index` authoritative throughout. **The finding is not "GIT_INDEX_FILE is dangerous" but "an undocumented workaround has undocumented costs"** — which is the general form and the reason this is a tools-lane row rather than a note. **(f) §9 gains (d) as a clause**, so the next session that reaches for the same shortcut meets the cost at the same place it meets the shortcut. | Principal correction 2026-08-15 (caught at source, before any further staging); `AGENTS.md` §9; CD-056; CD-083(a); `cb248be` | **RECORDED. `git reset` run, index clean, `cb248be` intact. §9 clause added.** |
| **TOOLS-CR-004** | 2026-08-15 | **`AGENTS.md` §9 DOCUMENTS THE ASIDE PRACTICE AND SPECIFIES NO CLEANUP STEP — and `.git/lock-debris/` turns out to grow with NO agent working at all. Found on the Principal's machine while executing TOOLS-CR-003's remedy; filed as its own row because `tools/CORRECTIONS.md`'s header rule is that rows are never edited — a row is added citing the old, never amended.** *(Next free verified at source: this ledger defines through `TOOLS-CR-003`, minted this session; no `TOOLS-CR-004` token existed anywhere in the repo.)* **(a) THE CLEANUP PROCEDURE, in order, because the order is the finding.** **`del /s /q .git\*.lock` FIRST · then `git gc --prune=now` · then `rmdir /s /q .git\lock-debris`.** **`gc` itself fails on stale locks**, so a cleanup that starts with `gc` cannot start at all — which is exactly what the agent hit in the sandbox (*"fatal: failed to run reflog"*). **(b) And the sandbox's own FAILED `gc` attempt leaves more locks than it started with.** It leaves **`gc.pid`** and **`packed-refs.lock`** behind. On the Principal's machine **five stale locks had to be cleared before `gc` would run, one error at a time** — each attempt surfacing exactly one lock, so the failure is serial rather than reported at once. **A cleanup step that cannot be run until a prior cleanup step has run, and that reports its blockers one per attempt, is a procedure and not a command** — which is why it is written out here rather than left as *"run gc"*. **(c) THE LOAD-BEARING FINDING: the debris is NOT all agent-created.** `.git/lock-debris/` held **145 entries dating to 2026-08-12, across three sessions, never once cleared.** **`maintenance.lock` entries are the largest group, and those come from git's OWN BACKGROUND MAINTENANCE, not from agent writes.** So **the folder grows even when no agent is working**, and every prior session's reading of it — including this session's, which reported the growth as *"the practice working as designed"* — **attributed to agent activity a majority that was never agent activity.** **(d) The gap in §9, stated precisely.** §9 documents the aside practice, names `.git/lock-debris/` as its only destination (CD-056), and says to move the lock aside **before each git write**. **It specifies no cleanup step, no owner and no cadence** — so the destination it mandates is a directory nothing is ever instructed to empty. **The practice was complete as a workaround and incomplete as a lifecycle**, and three sessions of accumulation is what made the omission visible. **(e) Executable: §9 gains the cleanup procedure at (a), with its ordering and its serial-failure behaviour, and states that cleanup is owed PERIODICALLY ON THE PRINCIPAL'S MACHINE rather than at the end of an agent session** — because (c) means an agent-session-end trigger would miss the largest source. The sandbox **cannot** perform any of it; the row records that as a standing limit, not a deferral. **(f) TOOLS-CR-003 is CITED, NOT EDITED**, per this ledger's own header rule. -003 is not wrong: it recorded a `GIT_INDEX_FILE` hazard and its remedy. This row records what executing that remedy revealed. | Principal, 2026-08-15, from the cleanup run on his machine; **cites TOOLS-CR-003, does not amend it**; CD-056; `AGENTS.md` §9; `tools/CORRECTIONS.md` header rule (rows are never edited) | **RECORDED. §9 clause added. Cleanup is the Principal's, on his machine, periodic.** |
| **TOOLS-CR-005 · PATTERN CANDIDATE** | 2026-08-15 | **A QUERY'S SILENCE IS EVIDENCE ONLY IF THE QUERY IS KNOWN TO BE COMPLETE. Raised by the Principal 2026-08-15; RAISED, NOT PROMOTED.** *(Next free verified at source: this ledger defines through `TOOLS-CR-004`, minted this session; no `TOOLS-CR-005` token existed anywhere in the repo.)* **The shape.** Every census, next-free check and existence sweep in this repo answers a question by **finding nothing**. *Nothing found* and *nothing there* are the same output and different facts, and **the gap between them is the completeness of the query — which the query itself never reports.** **INSTANCE 1 — the `find` predicate, 2026-08-15, this session.** Sweeping project knowledge for the chain's handoffs, the agent ran `find … -name "HANDOFF*" -o -name "STATUS*" -o -name "SCD_UNIFICATION*"`. **Unparenthesised `-o` binds the implicit `-print` to the last predicate only**, so four of five handoffs printed and `HANDOFF_2026-08-10_scd-central-migration.md` did not. **The agent read the gap as a missing file and was one step from recording it `NOT-IN-REPO` under CD-133** — a term minted three commits earlier for exactly this class of claim. Caught by re-running against a plain `ls`. **A malformed query and a real absence produce byte-identical output.** **INSTANCE 2 — CD-133's register census, and it is the POSITIVE instance, recorded because a pattern with only failures teaches nothing about what right looks like.** That row had to establish that the Unification Decision Register is **not in the repo**, which is a claim of absence and therefore only as good as the search behind it. **So the search was made visible rather than asserted**: every `UD-` token counted and reported in full (`UD-60` ×15 · `UD-23` ×11 · `UD-11` ×2 · `UD-09` ×2, four of sixty), `UD-01` shown to return zero, and `find` run against **three** independent name patterns. **The completeness was demonstrated in the row, not claimed in a sentence** — which is why CD-133's absence finding stands and Instance 1's did not survive ten minutes. **INSTANCE 3 — CD-087(b), verified at source before being counted here** *(the Principal asked; it belongs)*. A session-1 next-free check placed a row on the stated ground that *"CR-012 was verified free at source … no `CR-012` token existed anywhere in the repo."* **`canon/_wip/c5-math/CORRECTIONS.md` already held its own `CR-012`**, dated 2026-08-12, committed at `5634a5f` two days earlier, **with three live citations** in `C5_MATH_Source_06.md`. CD-087(b)'s own words: **"A next-free check that reports a number free when it is not is worse than no check — it converts a collision into a documented non-collision."** **Same shape: silence taken for absence, by a query that was not complete.** **WHY IT IS NOT PROMOTED, and this is deliberate.** The obvious gate — *make every census prove its own completeness* — **is not writable in general**: a query cannot know what it failed to ask. What is writable is narrower and belongs to whoever promotes this: **a next-free check that reads the LEDGER FILE rather than grepping tokens cannot miss a row the ledger defines**, and `ledger_check.py` already reads ledgers that way. **Enumerating the shape's instances is not yet the same as knowing what to build**, and CD-088's precedent is that a list hardens into a definition if promoted early. **Three instances is CD-088's promotion threshold, so this is at the threshold and is being HELD there by ruling, not by arithmetic.** | Principal, 2026-08-15; CD-133; CD-087(b) (verified at source); CD-088 (the PATTERN-promotion form); `ledger_check.py` | **CANDIDATE — 3 instances, at threshold, HELD by ruling. Not promoted; no gate proposed.** |
