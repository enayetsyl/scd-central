# tools/hub-export — SLOT

⬜ NOT YET SLOTTED

Vendor here: validate_import.py + the L1–L4 conformance harness from scd-hub, so artifacts
are born conformant at authoring time (Hub currently checks only at import time).
The Hub's LOCKED import contract v1.0 is the ONLY integration interface — nothing new is designed here.
