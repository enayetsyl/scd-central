# tools/assets — SLOT

⬜ NOT YET SLOTTED

Place here: sync.py (rclone wrapper) + assets_manifest convention (filename + hash per
consuming JSON). Large binaries (book images, scans) live on Google Drive via rclone,
NOT in this repo. Migrate to R2 only when a storefront launches (storybook venture concern).
